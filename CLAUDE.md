# Noctívago

Windows desktop ambient sound mixer. Electron + vanilla JS/DOM (no UI framework).

See `CONTRIBUTING.md` for build/run instructions and code conventions if you're working on the codebase.

## Architecture

- `src/main/` — Electron main process.
  - `index.js` — window creation, custom `sound://` protocol (serves either the original file or a rendered loop clip, see below) and `plugin://` protocol (serves plugin files, see Plugins below). Under `npm run dev` with more than one display connected, `devWindowPosition()` opens the dev window on the highest-numbered display rather than the default (primary, centered) position, so a dev session doesn't pop a window over whatever's already in use on the primary display — computed *before* window creation (passed as `x`/`y` to the `BrowserWindow` constructor) rather than moved after the fact, to avoid a visible flash on the primary display first. Gated on `!app.isPackaged`, so a real installed build is never affected.
  - `ipc.js` — IPC handlers.
  - `library.js` — registered sound files, persisted via `electron-store`.
  - `presets.js` — saved mixes.
  - `ffmpeg/` — ffmpeg integration (see below). `runFfmpeg.js` is the reusable spawn wrapper; `ffmpegPath.js` resolves the bundled binary; `loopClip.js` and `waveformPeaks.js` are its two current consumers.
  - `plugins/` — `registry.js` (scans + caches bundled/user plugin manifests), `protocol.js` (`plugin://` handler + path-traversal guard), `invoke.js` (dispatches `plugin:invoke` IPC to a plugin's own main-process module).
- `src/preload/index.js` — contextBridge API exposed to the renderer as `window.noctivago` (namespaces: `library`, `presets`, `audio`, `plugins`).
- `src/shared/constants.js` — plain constants imported by both main and renderer (e.g. `MAX_BUFFER_CLIP_SECONDS`).
- `src/renderer/` — plain JS/DOM UI, no framework.
  - `core/TabHost.js` — tab bar + lazily-mounted `<section>` per tab.
  - `core/PluginLoader.js` — discovers plugins via IPC, dynamically `import()`s each one, registers its tab(s).
  - `tabs/mixer/index.js` — the entire sound mixer, registered as the first tab via `mount(container)`.
  - `audio/AudioEngine.js`, `audio/SoundSource.js` (streaming playback), `audio/BufferSoundSource.js` (buffer/gapless playback).
  - `ui/*.js` — SoundList, SoundRow, PresetsModal (mixer-specific, used only by `tabs/mixer/`).
  - `main.js` — a tiny bootstrap: creates the `TabHost`, registers the Mixer tab synchronously, then calls `loadPlugins()`.

### Playback: two modes, chosen automatically per sound

**Stream mode** (`LocalFileSoundSource`, the default): each sound plays through **two** `<audio>` elements of the same file (`src="sound://<id>"`), one active and one standby, each piped through its own crossfade `GainNode` and then summed into the shared chain `→ highpass BiquadFilterNode → lowpass BiquadFilterNode → EQ chain → filter GainNode → per-sound GainNode → master GainNode` (echo taps off filter GainNode as before). The filter nodes apply the sound's saved highpass/lowpass/gain/EQ settings (same chain shape as the Remix plugin's live preview) — this is what makes filters audible even for sounds that never qualify for a baked buffer clip (see below). The `sound://` protocol hand-rolls HTTP Range support via `fs.createReadStream` (Electron's `net.fetch` on `file://` URLs was found to silently ignore Range headers in the Electron version this was built against — don't assume it works without verifying against the version in use). This is what makes arbitrarily long files (hours) playable at all without loading them into memory.

Looping is a two-element crossfade at the seam: a `requestAnimationFrame` `_tick` boundary-check watches the active element's `currentTime`; when it reaches `loopEnd - fade*speed` (the `*speed` since `fade` is wall-clock), the standby element (pre-seeked to `loopStart`) starts playing and the two crossfade GainNodes ramp in opposite directions over `fade` seconds, then a `setTimeout` (`fade*1000 + 40`) swaps their roles — the retired element is paused and rewound to `loopStart` to become the next standby. `fade` = `entry.crossfadeSeconds` (the *same* per-sound Remix "Loop crossfade" slider that drives the baked-clip crossfade; `null` → a local `DEFAULT_STREAM_CROSSFADE_SECONDS = 0.2` mirroring `loopClip.js`'s `DEFAULT_CROSSFADE_SECONDS`), clamped to a quarter of the loop region. **An explicit `crossfadeSeconds: 0` falls back to a plain `currentTime = loopStart` hard seek-back** on the single active element (not sample-accurate, audible click/gap on non-self-looping content — which is exactly why the crossfade exists). The seam trigger is still rAF-polled `currentTime` (imprecise by nature), but a ~200ms crossfade masks that jitter far better than a hard seek would — this is the streaming counterpart of buffer mode's baked tail/head crossfade, minus the sample-accuracy. This dual-element approach was chosen over a pre-baked ffmpeg bridge segment (no ffmpeg round-trip, no disk cache/staleness tracking, filters stay live, loop region stays exact, and it's automatic rather than opt-in). `pause()` mid-crossfade finalizes the swap synchronously so resume continues cleanly from just past the loop point; `dispose()` tears down both elements + both source nodes + both crossfade gains. Cost: two `<audio>` elements (and two `MediaElementAudioSourceNode`s) per stream-mode loop sound — negligible in practice since most sounds get baked into buffer mode anyway and only a genuinely long *loop region* (>600s) keeps a sound in stream mode for real.

**Buffer mode** (`BufferSoundSource`): whenever the user trims + sets filters and hits Save in the Remix plugin (see below), ffmpeg renders *just that loop region*, with any highpass/lowpass/gain filters baked in via `-af`, out to a small WAV (`src/main/ffmpeg/loopClip.js`, `getUserData()/clips/<id>.wav`) — but only if the region is ≤ `MAX_BUFFER_CLIP_SECONDS` (600s/10min, in `src/shared/constants.js`). Since the clip is now short regardless of how long the source file was, it's fully decoded client-side (`decodeAudioData`) into a native `AudioBufferSourceNode` with true `loop=true`/`loopStart`/`loopEnd` — sample-accurate, click-free. `tabs/mixer/index.js`'s `loopClipEligible(entry)` checks `entry.loopClipReady` + that the clip's recorded loop points *and* filters still match the entry's live ones (a staleness check, since either changing without re-saving should fall back to streaming) before trying buffer mode, and falls back to stream mode on *any* failure (missing ffmpeg, corrupt clip, decode error) — buffer mode is strictly additive and only ever affects loop-seam smoothness, never whether filters apply, never required for the app to work.

`BufferSoundSource.pause()` tracks elapsed offset manually (`AudioBufferSourceNode` is one-shot — can't be pause/resumed like `<audio>` can) so resuming continues from the same position rather than restarting, matching stream mode's behavior.

**Getting a sound into buffer mode**: the default is lazy — `tabs/mixer/index.js`'s `maybeAutoBake` bakes a full-file clip the first time an eligible, never-baked sound actually plays. A Settings toggle (`eagerlyBakeOnImport`, off by default) makes this eager instead: `library.js`'s `maybeEagerBakeOnImport`, fired fire-and-forget from `addSound()` — the single choke point every import path (single file, folder-as-preset, folder-as-tag, watch-folder) already funnels through — probes the file's real duration via ffmpeg directly (`loopClip.js`'s `probeDurationSeconds`, since a fresh import has no `durationSeconds` yet) and bakes immediately, before the sound is ever played. Same `MAX_BUFFER_CLIP_SECONDS` cap and bake call as the lazy path — this only changes *when* the first bake happens, not how.

### Playback mode: Loop vs. Random Interval ("scatter")

Every sound's `playMode` (`'loop'` default, or `'scatter'`, in `library.js`) picks between the two source families above (continuous loop) and a second pair — `BufferScatterSource`/`StreamScatterSource` (`src/renderer/audio/ScatterSoundSource.js`) — that instead replay the trimmed clip as one-shots on a randomized gap (`entry.scatter.minGapSeconds`/`maxGapSeconds`) with a randomized pitch each time (`entry.scatter.minPitchSemitones`/`maxPitchSemitones`). As of v0.1.105, `0`/`0` means literally no pitch change — a separate explicit `entry.scatter.pitchFullyRandom` checkbox (mirrored by `gapFullyRandom` for the gap) is the only way to get a fully-random roll across the whole ±12 st range; before v0.1.105, `0`/`0` secretly meant fully random, which is no longer true and shouldn't be assumed from older notes. This mirrors the game-engine "scattered ambience emitter" pattern (Wwise Random Container, FMOD's Scatterer Instrument, Unity's Audio Random Container) for a short, event-like sound (a creak, a bird call) inside a longer ambient mix, rather than looping it back-to-back. `tabs/mixer/index.js`'s `desiredSourceKind(entry)` crosses `playMode` with buffer eligibility into four kinds (`loop-buffer`/`loop-stream`/`scatter-buffer`/`scatter-stream`); `reconcileSource` swaps the live source whenever the desired kind or the bake itself changes, same pattern the original loop/stream swap already used.

`BufferScatterSource` uses the decoded clip's native `AudioBufferSourceNode.detune` (cents) for pitch — true independent pitch shift with zero effect on shot duration or the gap timer, a nicer primitive than the playbackRate-linked pitch/tempo coupling most non-web engines settle for. `StreamScatterSource` (the fallback for a trim too long to bake) has no such primitive on `<audio>`, so it reuses `playbackRate` the same way Speed does elsewhere in this app — a pitch shift there also changes shot tempo, a documented, accepted limitation.

**Stream fallback filters (BUG FIX, v0.1.192)**: `StreamScatterSource`/`StreamScheduledSource` are the fallback used whenever a scatter/scheduled sound isn't (yet, or ever) playing from a baked clip — most commonly right after an edit invalidates the old bake, before the sound's next play re-bakes it. Until v0.1.192 these two only ever built highpass/lowpass/gain nodes, silently dropping Echo, Reverb, and the parametric EQ for as long as a sound stayed in this fallback — while `BufferScatterSource`/`BufferScheduledSource` always has the full set (baked into the clip's own samples at Save time) and the export pipeline always bakes the full set too (`loopClip.js`'s `buildFilterChain`/`applyReverbPass`), so the same sound could genuinely sound different in the Mixer vs. an export (reported directly: a thunderclap's export had "a really strong reverb that isn't audible on the mixer tab"). `src/renderer/audio/StreamFilterChain.js` is a small shared module (both Stream* classes are core, not plugins, so sharing one implementation is correct here, unlike the plugin sandbox's forced duplication elsewhere) building the same highpass → lowpass → EQ → gain → [echo tap] → [reverb tap] shape `LocalFileSoundSource` (`SoundSource.js`) already uses live, reusing its exported `eqNodeType`/`eqBandStageCount` helpers rather than a third copy.

Scatter-mode clips reuse `renderLoopClip` unchanged rather than a second bake pipeline: both `library.js` and `tabs/mixer/index.js` have an `effectiveCrossfadeSeconds(entry)` helper that forces the crossfade to `0` whenever `playMode === 'scatter'` (crossfade+rotation, which permanently reorders the clip's own audio to hide a *looping* wrap point, would be actively wrong for a clip that's never looped back-to-back) — `0` already means "plain trim, no rotation" in `loopClip.js`'s `doRender`, so this is a pure input-shaping trick, not new ffmpeg logic. Switching a sound between Loop and Random Interval in the Remix plugin (which owns this control, alongside trim/filters/crossfade/speed-pitch — the same "shape a sound" editor as everything else that affects the bake) naturally invalidates the old bake via the same staleness check every other trim/filter edit already goes through.

**"Bias toward a value" (v0.1.195)**: owner inbox request — min/max stay the hard range for a scatter/scheduled per-shot random pick, but an optional bias value should win more often than a plain uniform draw. Rather than a new distribution, this reuses Fluctuation's own already-tuned "sits near a target most of the time, wanders to the extremes occasionally, toward whichever side has more room" math (`Modulator.js`'s `_pickTarget`) — pulled out into a standalone exported `pickBiasedValue(min, max, bias)` that `_pickTarget` itself now calls, so the two features share one implementation rather than two independently-feeling distributions. `ScatterSoundSource.js`'s `randomGapSeconds`/`randomPitchSemitones` (the latter shared by `ScheduledSoundSource.js`) call it when `gapBiasEnabled`/`pitchBiasEnabled` is on, clamping the stored bias into the current `[min, max]` first in case the range shrank since it was set. Off by default (`gapBiasEnabled`/`pitchBiasEnabled: false`) — an existing sound's random-pick behavior is untouched unless the owner explicitly turns it on, the same "explicit opt-in checkbox, not inferred from field values" precedent `gapFullyRandom`/`pitchFullyRandom` themselves already established (and `fullyRandom` still wins outright if both are somehow on, since it bypasses min/max/bias entirely). `plugins/export/simulate.js` carries its own duplicated copy of `pickBiasedValue` + the two random-pick functions (plugin sandbox, can't import outside its own directory) — keep in sync if this math changes again. Verified live via CDP against a real dev build (UI elements render and wire correctly, the bias checkbox correctly enables/disables its own number field and is itself grayed out whenever Fully-random overrides it, a save round-trips the new fields through both the per-preset-override and shared-baseline write paths correctly) plus new unit tests on `pickBiasedValue` itself (stays in bounds, clusters near bias far more than a uniform pick would in a same-width window, reaches both edges even off-center, and the degenerate `min === max === bias` case doesn't produce `NaN`/`Infinity`).

### Waveform

`src/main/ffmpeg/waveformPeaks.js` streams the *whole* original file through ffmpeg as downsampled mono float32 PCM and reduces it into `[min,max]` peaks per pixel column with O(1) memory (never buffers the decoded audio) — works for files of any length/format. This replaced an earlier renderer-side `decodeAudioData`-based approach that was capped at 30 minutes and fell back to a bare time-ruler beyond that.

**Canvas resolution**: any canvas whose bitmap resolution (`width`/`height` HTML attributes) doesn't match its rendered CSS size stretches/blurs when the browser scales it up. `waveform.js`'s and `EqEditor.js`'s `resizeCanvasForDisplay()` resize the canvas's real bitmap resolution to its rendered CSS size × `devicePixelRatio` before every draw and scale the context so the rest of the drawing code keeps working in plain CSS-pixel coordinates. Interaction math (drag, wheel, hit-testing) works in `rect.width`/`rect.height` (CSS-pixel space) throughout — only the *drawing* functions needed the resize fix, a distinction worth keeping in mind when adding a new canvas-based control (see the EQ editor note below).

**Live spectrum analyzer**: `PreviewSource.js` taps an `AnalyserNode` (never connected onward — a pure read tap) off `outputGainNode`, the single point both crossfading voices' signals combine into, so it reflects whichever voice is actually audible regardless of crossfade state. `EqEditor.js`'s `tickSpectrum(analyserNode)` is called once per frame from the same `tickPreviewPlayhead()` rAF loop that already drives the waveform playhead and sticky progress bar (no separate loop) — reads `getByteFrequencyData()` (already dB-scaled 0-255 by the Web Audio API itself) and redraws a filled area connecting each linearly-spaced FFT bin's point via the graph's own log-frequency `freqToX`. Takes over from a static band-energy backdrop (below) whenever playing; `clearSpectrum()` (called on pause/stop/dispose) falls back to the static backdrop rather than freezing on the last live frame.

**Per-band filter types**: 7 EQ bands, each independently one of 8 types via a `type` field on the band object (`'off' | 'lowpass' | 'highpass' | 'bandpass' | 'notch' | 'lowshelf' | 'highshelf' | 'peaking'`), selected via a `<select>` next to the Freq/Gain/Q precision fields. Web Audio's native `BiquadFilterNode.type` covers 7 of the 8 shapes directly — only `'off'` is a pseudo-type (not a real biquad shape), mapped to `'allpass'` at the node level (`eqNodeType()` in both `SoundSource.js` and `PreviewSource.js`) since allpass leaves the amplitude spectrum completely unchanged, a true no-op without needing to dynamically rewire the fixed per-band node chain. `.type` isn't an AudioParam (a plain property, can't be smoothed like frequency/Q/gain) — only reassigned when it actually changes, to avoid resetting the filter's internal state on every unrelated update. Baking dispatches per band to the matching ffmpeg filter (`loopClip.js`'s `buildEqBandFilter`: `lowpass`/`highpass`/`bandpass`/`bandreject`(notch)/`bass`(lowshelf)/`treble`(highshelf)/`equalizer`(peaking), `'off'` simply omitted from the chain). Only peaking/lowshelf/highshelf are true no-ops at `gainDb: 0` (their whole effect is proportional to gain); lowpass/highpass/bandpass/notch have no gain parameter at all and always shape the signal whenever selected, so they're never skipped just for looking "neutral."

### Why ffmpeg, not more JS

Both the loop-seam click and the waveform-for-long-files problems trace back to the same root cause: `decodeAudioData` on a whole multi-hour file either throws `EncodingError` or uses gigabytes of RAM (this is *why* stream mode exists at all). ffmpeg (bundled via `ffmpeg-static`, spawned via `child_process`, never a shell) processes files without holding them fully in memory, sidestepping that limit entirely. `runFfmpeg.js` is deliberately generic/domain-agnostic so ffmpeg-based features reuse it rather than each rolling their own spawn logic.

**Packaging note:** `electron-builder.yml` has `asarUnpack: [node_modules/ffmpeg-static/**]` — required because binaries can't execute from inside an asar archive.

**ffmpeg binary license note**: the bundled ffmpeg binary (fetched via `ffmpeg-static`) is GPLv3, not the more permissive LGPL — confirmed via the binary's own accompanying license file (`node_modules/ffmpeg-static/ffmpeg.exe.LICENSE`/`.README`, which `ffmpeg-static` itself started shipping after [eugeneware/ffmpeg-static#8](https://github.com/eugeneware/ffmpeg-static/issues/8)). See `THIRD_PARTY_NOTICES.md` for the full attribution, exact version/source-commit link, and why bundling a GPLv3 binary doesn't require Noctívago's own Apache-2.0 code to relicense (it's always invoked as a separate subprocess via `runFfmpeg.js`, never linked).

### App icon

`resources/icon.ico` is hand-built by `resources/icon-src/build-ico.mjs` (run `node resources/icon-src/build-ico.mjs` from the repo root to regenerate after editing `resources/icon-src/render.html`'s SVG and re-rendering `icon-master.png`) rather than a plain PNG electron-builder auto-converts. **Why this matters**: electron-builder's own PNG→ICO conversion PNG-compresses every embedded size, but Windows only honors PNG compression for the 256px entry — sizes below that (16/24/32/48/64, exactly what the taskbar and Windows Search use) silently fall back to a generic icon if they're PNG-compressed instead of classic uncompressed BMP/DIB data. The build script generates real BMP/DIB data (with a proper 1bpp AND mask derived from the alpha channel) for every size below 256, PNG-compressing only the 256 entry. `src/main/tray.js` (system tray) and `src/main/index.js` (`BrowserWindow`'s `icon` option) both read the same design; the tray one stays a plain PNG since `nativeImage`-based tray icons don't have this multi-size ICO requirement.

## Plugins

The app is designed for outside contributors to add functionality as plugins rather than editing core — modeled on Obsidian's plugin architecture, but **deliberately not** copying its trust model (Obsidian plugins get raw Node/Electron access; here that's split by process, see below), since this codebase is more sandboxed (`contextIsolation: true`, no `nodeIntegration`, everything served through custom protocols rather than raw file access).

**A plugin is a folder** containing `manifest.json` (`id`, `name`, `version`, `main` required; `mainProcess` optional) + a `main` entry file exporting a default class:
```js
export default class MyPlugin {
  constructor(app, manifest) { this.app = app }
  async onload() { this.unregister = this.app.tabs.register({ id, title, mount: (container) => {...} }) }
  async onunload() { this.unregister?.() }
}
```
Two real examples exist in `plugins/`: `hello-world/` (minimal working plugin) and `broken-plugin-example/` (deliberately throws in `onload()` — a regression-test fixture proving one broken plugin can't take down the app or other plugins).

`register()` also accepts an optional `mountSticky: (container) => {...}` alongside `mount` — its own section in the shared sticky bar directly under the tab bar (`#tab-sticky-bar` in `index.html`, wired in `TabHost.js`), always visible regardless of how far the tab's own content is scrolled. This is a fixed-height flex sibling of `main` in `#app`'s layout, not `position: sticky` — deliberately simpler, no scroll-container edge cases to get wrong. Distinct from the toolbar above the tab bar (global mix Play all/master volume, app-wide): the sticky bar is per-tab, for controlling whatever's local to that specific tab (e.g. the Remix plugin's own trim preview, see below) — only the active tab's sticky section is shown, and the shared bar itself hides entirely for a tab that doesn't provide one (like the Mixer, which has no single "current preview" the way Remix does). Same lazy-mount-once-then-show/hide lifecycle `mount` already has, not remounted on every tab switch.

**Where plugins live**: `plugins/<id>/` at the repo root for bundled/official plugins (packaged into the app via `electron-builder.yml`'s `extraResources`), and `<userData>/plugins/<id>/` for user-installed ones (parallel to Obsidian's `.obsidian/plugins/`). `src/main/plugins/registry.js` scans both and merges them; on an id collision, bundled wins. Presence in either directory *is* enabled — there's no enable/disable toggle UI in v1 (disable by moving the folder out).

**How loading actually works**: `plugin://<id>/<path>` is a privileged `standard` Electron scheme (registered in `main/index.js`, handled in `src/main/plugins/protocol.js`) serving a plugin's own files as real ES modules — `PluginLoader.js` in the renderer does `await import(\`plugin://${id}/${manifest.main}\`)`, which resolves relative imports between a plugin's own files correctly and shows real filenames/breakpoints in DevTools. This directly extends the same pattern `sound://` already established for serving local files into the sandboxed renderer.

**Security posture — split by process**:
- Renderer-side plugin code (`manifest.main`) is exactly as sandboxed as core code — no `require`, no `ipcRenderer`, only the curated `app` object (`{ tabs, noctivago, plugin }`) it's constructed with. `app.noctivago` is `window.noctivago` handed through wholesale (already curated/whitelisted) — no per-plugin capability scoping in v1, any enabled plugin can call any core API, matching Obsidian's own trust model closely enough not to need more yet.
- Main-process plugin code (`manifest.mainProcess`, opt-in) is ordinary trusted Node — same trust level `src/main/*.js` already has — loaded via `src/main/plugins/invoke.js`. This is the only place a plugin gets real power (spawn ffmpeg, touch disk), reached from the renderer via one generic `api.plugins.invoke(pluginId, method, ...args)` rather than each plugin needing its own hand-written IPC channel in `ipc.js`.
- This is not real code sandboxing — a `mainProcess` plugin still has full Node power once opted into, same as Obsidian's actual model. Trust boundary remains "the user reads/trusts plugin source before installing," just scoped to a smaller, more visible manifest field than "the whole plugin."

**Explicit non-goals for v1** (deferred, not designed): plugin marketplace/browser, hot-reload dev tooling, settings-tab API, command palette, ribbon icons, enable/disable UI, per-plugin capability scoping, persistent per-plugin data storage.

### The Remix plugin (`plugins/editor/`, tab titled "Remix")

The first real (non-toy) plugin, and the one that validated the plugin API against an actual feature. All per-sound editing — trim points *and* filters (highpass, lowpass, gain) — lives here.

- **Live preview without touching ffmpeg**: `audio/PreviewSource.js` streams the sound the same way `LocalFileSoundSource` does (`sound://<id>`, works for any file length) but inserts a `BiquadFilterNode` (highpass) → `BiquadFilterNode` (lowpass) → `GainNode` chain before the output gain. Dragging a filter slider calls `setTargetAtTime` on these nodes directly — instant, click-free, audible feedback with zero ffmpeg round-trips. ffmpeg only runs once, on Save.
- **Save bakes trim + filters in one ffmpeg pass**: reuses `loopClip.js`'s renderer (extended to accept a `filters` object and insert `-af highpass=f=…,lowpass=f=…,volume=…dB` into the ffmpeg args), rather than a second, parallel baking pipeline. The Mixer's buffer-mode eligibility check compares filters as well as loop points, so changing either without re-saving correctly falls back to streaming (still filtered — see Playback above — just with a small loop seam) instead of playing a stale baked clip.
- **Save never touches the original file**: trim points and filters are persisted as metadata (`library.js`, via `electron-store`) and applied live by both playback modes; the ffmpeg render is a disposable derived cache (`getUserData()/clips/<id>.wav`), regenerated on every Save, not an edit to the source audio. The Save button surfaces the outcome (baked / bake failed / skipped because the trim exceeds the buffer-mode cap) via a status line under the button.
- **BUG FIX, v0.1.193 — a baseline Save had no live-reconcile bridge to the Mixer.** `performSave` writes to one of two places: a preset override (`presets:updateSoundOverride`, when the open sound is a member of the preset the editing session is pinned to) or the shared baseline (`library:update*`, otherwise). Only the override branch dispatched a `noctivago:sound-override-changed` CustomEvent telling an already-mounted Mixer tab to patch its cache and `reconcileSource()` the live playing source (the same bridge `noctivago:sound-fluctuation-changed` uses for Fluctuation) — the baseline branch dispatched nothing at all, and `library:changed` (the IPC push channel `tabs/mixer/index.js` already listens for) is watch-folder-only, never fired by any of the `library:update*` handlers. So a baseline-path edit — trim, filters/EQ, crossfade, Speed/Pitch, play mode, scatter, or schedule — was correctly persisted to disk but a sound already playing in the Mixer kept its old in-memory settings until something else happened to rerun `refreshList()` (switching tabs away from Remix and back). Reported directly: disabling a scatter sound's per-shot pitch/speed range "still kept pitch randomized and also speed." Fixed by giving the baseline branch the same event bridge: it now dispatches `noctivago:sound-baseline-changed` with the full patch, and `tabs/mixer/index.js` patches `state.baselineById` (the raw cache `recomputeEffectiveLibrary()` rebuilds `state.library` from — mutating the derived `state.library` entry directly wouldn't survive the next recompute), then recomputes and reconciles, mirroring the override listener's own shape exactly. Verified live via CDP against a real dev build: seeded a live `BufferScatterSource` with a randomized scatter config, persisted a disabled config to disk (confirming the live source stayed stale, reproducing the bug), then fired the new event and confirmed the same live source instance picked up the disabled config immediately. In the same pass, corrected a stale doc claim just above (§ Playback mode: Loop vs. Random Interval) that still described pre-v0.1.105 behavior (pitch `0`/`0` "secretly" meaning fully random) — that was fixed by v0.1.105's explicit `pitchFullyRandom` checkbox and the doc had never been updated to say so.
- **BUG FIX, v0.1.194 — v0.1.193's own live-reconcile bridge fired too early and permanently downgraded the sound's playback quality.** `performSave`'s call order is: (1) persist the edited metadata + dispatch the `sound-override-changed`/`sound-baseline-changed` bridge event, (2) *then* call `this.api.audio.renderLoopClip(...)` to actually re-bake the clip (an async ffmpeg round-trip). v0.1.193 added the bridge dispatch at step 1, before the bake at step 2 had even started — so by the time the Mixer's listener ran `reconcileSource()`, `entry.speedPitch`/`entry.filters` already reflected the new edit but `entry.loopClipSpeedPitch`/`loopClipFilters` (the *previous* bake's own recorded settings) hadn't been touched yet. `loopClipEligible()` correctly saw a mismatch and concluded the existing bake was stale, so `desiredSourceKind()` flipped from `loop-buffer` to `loop-stream` — and nothing ever flipped it back, because the real bake finishing a moment later only updated the Remix plugin's own local `this.currentEntry.loopClip*` fields, never the Mixer's separate `state.baselineById` cache. Stream mode has no live pitch-shift primitive at all (`reconcileSource`'s `'loop-stream'` branch only applies filters/loop points/Speed/crossfade — see Playback above), so Pitch silently stopped applying, permanently, for any sound edited this way — along with anything else that only ever takes effect through the baked clip (Doppler, Reverse, Noise reduction), and the loop lost its sample-accurate seam. Reported directly, several ways at once: "I change audio settings like pitch and speed and it has no results on live playback on mixer tab," Noise reduction/EQ "seemingly aren't working," and exports sounding "dramatically different from live playback" (the export always re-bakes from the true current settings, so it was the Mixer's stuck fallback that was wrong, not the export). Fixed by moving the event dispatch to fire only once the bake has actually resolved (success, failure, or skipped for being over the buffer-mode cap or not bake-relevant), and by having its payload carry a `loopClip` sub-object (`ready`/`start`/`end`/`filters`/`crossfadeSeconds`/`speedPitch`) mirroring the just-updated `this.currentEntry.loopClip*` fields. Both Mixer listeners now route that through a shared `patchBaselineLoopClip()` helper that updates `state.baselineById`'s `loopClip*` fields regardless of which event fired — necessary because the baked clip file and its bookkeeping always live on the *shared* baseline record (`library.js`'s `setLoopClipReady`, called from the same `audio:renderLoopClip` IPC handler either way), never per-preset-override, even when the edit that triggered the bake came through the override branch. Verified live via CDP against a real dev build: reproduced the exact bug (a `loop-buffer` source visibly flipped to `loop-stream` immediately after simulating the old pre-fix event timing, and stayed there even after a real matching bake completed afterward with no further event); then confirmed the fix's actual flow (metadata write → real `audio.renderLoopClip` → the now-delayed, loopClip-carrying event) correctly returned the same sound to `loop-buffer` with `loopClipEligible()` true and the new pitch reflected in `loopClipSpeedPitch`. Full 51-test suite and a clean build pass.
- **BUG FIX, v0.1.196 — two data-integrity bugs in `performSave`/its Mixer listeners, caught by self-review before either was reported.** (1) **Cross-sound save race**: `performSave` read `this.currentEntry`/`this.editingPresetId`/`this.fluctuation` *after* its `await`s (the metadata IPC writes, the ffmpeg bake) — but nothing stopped the user from clicking a different sound in the Remix picker while a save/autosave was still in flight (`loadSound()` reassigns `this.currentEntry` synchronously with no lock against an in-flight save; `_saveInProgress` only guards two overlapping `save()` calls, not a save racing a sound switch). A bake that resolved after the switch wrote its results onto the *new* sound's `currentEntry` (corrupting it with the old sound's bake data) and dispatched a Mixer event mislabeling the old sound's id under the new sound's preset context. Fixed by capturing `entry`/`savingEditingPresetId`/`savingFluctuation` once at the top of `performSave`, before any `await`, and mutating/reading only those captured references from then on; a new `stillEditingThisEntry()` guard (`this.currentEntry === entry`) gates everything that's legitimately UI-instance-scoped instead (status text, `savedSnapshot`, the Save button, the `bakedPreview` swap) so those don't stomp on whatever sound the user has since switched to. Also wrapped the `renderLoopClip` call in try/catch (normalizing a thrown rejection into the same `{ok:false}` shape a failed render already produces) so a real IPC/ffmpeg exception can't skip `notifyLiveReconcile()` and leave the Save button stuck on "Saving…" forever - the same failure class as v0.1.194, just via an exception instead of a timing gap. (2) **Preset-mismatch skipped the baseline patch**: the `sound-override-changed` listener's `presetId !== state.activePresetId` early return sat *before* `patchBaselineLoopClip` (v0.1.194's own fix), so saving an override for a preset other than the one currently loaded in the Mixer never synced `state.baselineById`'s `loopClip*` bookkeeping — even though the physical clip file (`getUserData()/clips/<id>.wav`) is shared across every preset and gets overwritten regardless of which one is active. If the stale baseline values happened to still match whichever preset *was* active, `loopClipEligible()` would wrongly report the bake valid and buffer-mode would play a clip that now actually contained a *different* preset's baked audio, with no staleness indicator. Fixed by moving `patchBaselineLoopClip` before the preset check — it always runs now, since the physical file was overwritten unconditionally; only the live-reconcile (which only matters for whatever's actually audible right now) stays gated on the active preset. Also merged the `sound-baseline-changed` listener's two separate `state.baselineById` get→spread→set round-trips into one (`loopClipPatchFields()` now returns a plain object both listeners spread into their own single `.set()`), and moved 4 new `pickBiasedValue` unit tests (added in v0.1.195) out from under `describe('Modulator', ...)` into their own top-level block, matching this test file's one-describe-per-exported-function convention. Both data-integrity fixes verified live via CDP against a real dev build (fired a save on sound A without awaiting it, immediately switched to sound B, confirmed B's `currentEntry` stayed uncorrupted and A's own bake still completed correctly; dispatched a `sound-override-changed` event with a deliberately mismatched `presetId` and confirmed `state.baselineById` still updated). Full 55-test suite and a clean build pass.
- **BUG FIX, v0.1.197 — the "Saved audio" preview toggle looked available but usually failed to actually play.** Reported directly: "never works as it should," happening on essentially every sound, with the only known workaround being a small edit followed by a re-save. Root cause found live via CDP, not by re-reading the v0.1.142/193/194 diffs (which looked correct in isolation): the toggle's own client-side gate (`savedPreviewEligible()`) deliberately only checks `loopClipReady` + a `bakedPreview` instance existing — staleness (an edit since the last bake) was intentionally *not* disqualifying, per v0.1.142's own design ("the toggle is now sticky... shows a dashed 'stale' outline rather than auto-reverting"). But the actual audio bytes for both "Saved audio" (`BakedClipPreview.js`) and the Mixer's own buffer-mode fetch come from the same `sound://<id>?variant=clip` URL, and the main process's handler for it (`getLoopClipPathForId`) ran the *strict* staleness comparison — the same one used to decide whether the Mixer can trust the cached clip at all — and returned `null` (a 404) the instant *any* live field diverged from what was actually baked, regardless of who was asking or why. For the Mixer this is inert (it already runs the identical comparison client-side in `loopClipEligible()` *before* ever choosing to fetch the URL, so it simply never asks while stale) — but Remix's "Saved audio" toggle explicitly *wants* to keep playing the last real bake through staleness, and instead got a hard 404 → `<audio>` element stuck in `DEMUXER_ERROR_COULD_NOT_OPEN`/`NETWORK_NO_SOURCE`, indistinguishable from broken. Since a sound is "stale" the moment almost anything is edited without an intervening re-save, this fired on nearly every real editing session — matching "essentially always" — and a resave "fixed" it only by re-syncing the two sides, not by fixing anything. Confirmed live: loaded an already-saved sound with no edits at all this session, whose baseline `filters.volumeEnvelope` had merely drifted from what was actually baked — toggle showed enabled, `<audio>` element was in the exact error state above. Fixed by splitting the one overloaded question `getLoopClipPathForId` was answering into two: a new `requireFresh` option (default `true`, unchanged behavior) gates the strict comparison; the `sound://` protocol handler's own call (serving *both* Remix's and the Mixer's requests for `variant=clip`) now passes `requireFresh: false` — "does a clip exist to play, stale or not" — while `ipc.js`'s export-time direct call (deciding whether a cached clip can be *reused* instead of re-baked) keeps the strict default, so export still never silently reuses stale audio. Verified live via CDP against a real dev build: the same known-stale sound's Saved-audio toggle now loads (`readyState: 4`, no error) and genuinely plays (`currentTime` advancing); confirmed the Mixer's own buffer-vs-stream decision is untouched code, unaffected by the relaxed protocol gate. Full 55-test suite and a clean build pass.
- **BUG FIX, v0.1.199 — Fluctuation's "Transition" field couldn't actually reach 0 (instant).** Reported directly: setting the transition period on volume/pitch Fluctuation to 0 should mean no transition at all, just the change happening immediately. Two independent floors stood in the way: `Modulator.js`'s `resolveFluctuationTiming`/`configure()` clamped `transitionSeconds` to a 0.2s minimum and additionally floored the derived time constant `tau` (`= transitionSeconds / 3`) at `Math.max(0.05, ...)`, so even a 0 input still glided over a short but nonzero window; separately, the Remix UI's own `flucSeconds()` input reader treated `0` as "not a valid positive number" and silently replaced it with the 8s default before it ever reached the Modulator. Fixed both ends: `flucSeconds()` now takes an `allowZero` flag (true only for the Transition field, not "Change every") so a typed `0` round-trips as `0` instead of reverting to a stale default; the timing clamps everywhere now floor at `0` rather than `0.2`/`0.05`. `Modulator._tick()`'s glide step `value += (target - value) * k` already reduces to an exact snap (`k = 1`) once `tau` is genuinely `0` — `Math.exp(-dt / 0)` is `0` for any `dt > 0`, and `dt` is never `0` in practice — but the tick loop now special-cases `tau > 0` explicitly rather than relying on that IEEE-754 behavior, since a coincidental `dt === 0` tick would otherwise divide `0/0` into `NaN`. This mirrors across every duplicated copy of the same timing/walk math per the usual cross-plugin/cross-process sync rule: the live core `Modulator` (`src/renderer/audio/Modulator.js`), the Remix plugin's own bundled copy (`plugins/editor/audio/Modulator.js`), the preset-level normalizer (`src/main/presets.js`'s `normalizeMixFluctuation`), and both export-time baked-walk generators (`src/main/ffmpeg/gainEnvelope.js`, `src/main/ffmpeg/pitchEnvelope.js`) — so 0 means instant consistently in live Mixer playback, Preset/Group-mode Fluctuation, and exports alike, not just Sound mode. Verified directly against the real `Modulator` class (not a reimplementation): forcing a target change with `transitionSeconds: 0` lands the very next tick at the exact target value (`0.8999999999999999` vs. `0.9`, float-epsilon only), confirmed by a new regression test (`test/modulator.test.js`). Full 60-test suite and a clean build pass.
- **BUG FIX, v0.1.200 — self-review caught a real edge case in v0.1.199's own fix before anyone hit it.** A `/code-review` pass over v0.1.197-199 (4 parallel finder agents) flagged that `flucSeconds()`'s new `allowZero` flag (added in v0.1.199 so a typed `0` in the Transition field round-trips as instant instead of falling back to the 8s default) didn't distinguish an actual `0` from a *blank* field — `Number('')` is `0` in JS, and `0` is finite, so clearing the field (e.g. to retype a new value, or just tabbing away mid-edit) silently saved `transitionSeconds: 0` instead of falling back to the default the way every other blank fluctuation-timing field already does. Fixed by checking for an empty/whitespace-only string before the `Number()` coercion rather than after it, so a blank field still falls back to `fallback` while an explicit `'0'` still passes through as `0`. Verified directly against the extracted function logic across every relevant case (blank, whitespace-only, explicit `'0'`, a normal value, and the non-`allowZero` fields' blank/zero behavior staying unchanged) plus the full 61-test suite and a clean build. Three other review findings were logged to the Board as follow-ups rather than rushed in here, since none were confirmed-reproducible regressions: the recursive watch-folder scan (v0.1.198) has no cycle guard against a Windows directory junction/OneDrive placeholder loop; that same scan runs synchronously on the Electron main process with no size cap, a real startup-latency risk for a very large watched library; and the manual "Add folder as tag/preset" import dialogs stayed flat (non-recursive, no auto-tagging) while watch-folder import went recursive in v0.1.198, an inconsistency that may or may not have been intentional.
- **BUG FIX, v0.1.202 — a repeat report of the Live-edit/Saved-audio toggle "not working properly," this time a genuinely different root cause from v0.1.197's fix.** `LoopEditor.js`'s `setLoopPoints(newStart, newEnd)` always clamps and calls its registered `onLoopChange` callback, which `index.js` wires to `previewSource?.setLoopPoints(...)` (the live Web Audio engine's own loop bounds) alongside updating the Start/End/Length boxes — but only the drag-on-waveform path actually called `loopEditorController.setLoopPoints(...)` at all. `applyManualLoopPoints()`/`applyManualLength()` (typing an exact Start/End/Length) and `suggestLoopPoint()` ("Suggest a loop point") each called `loopEditorController.setLoopPoints(...)` too — updating the displayed boxes and the controller's own state correctly — but `applyEditorState()` (undo/redo) bypassed it entirely, setting a raw local variable with no callback at all, so undo/redo didn't even update the Start/End boxes. Investigated live via a synthetic ~8.5-minute test WAV in a scratch dev profile (not the owner's real library) after their screenshot showed the sticky time readout stuck at a stale, halved duration following an edit — reproduced by driving the actual DOM (typing into the trim boxes, clicking Suggest) via CDP, then confirmed the exact mechanism with a temporary `window.__remixDebug = this` hook in `mount()` (removed before commit) that exposed `previewSource.loopStart`/`loopEnd` directly: after a typed edit, the boxes and `loopEditorController` showed the new region but `previewSource`'s own fields still held the old one, meaning **Live edit's actual audible loop bounds silently stayed wherever they were last set by a drag (or the sound's loaded values) regardless of any later typed/suggested edit** — while Saved audio (always a fresh bake from the current metadata) correctly reflected the new trim, so the two could sound completely different. Separately, none of these paths ever called `updateStickyProgress()`, so the sticky time readout only ever self-corrected once real playback's own rAF tick loop happened to run — editing loop points while paused left an arbitrarily stale number on screen (this is what the owner's screenshot actually showed: boxes reading the new trim, sticky readout still showing a stale duration from before). Fixed by adding one shared `commitLoopPoints(loopStart, loopEnd)` method (`updateLoopTimeInputs` + `previewSource?.setLoopPoints` + an immediate `updateStickyProgress` + `updateSaveButtonState`) wired as `loopEditorController`'s one and only `onLoopChange` callback, so every caller needs to do nothing more than call `loopEditorController.setLoopPoints(...)` itself — the callback fires synchronously and keeps everything in sync automatically; undo/redo's `applyEditorState()` now goes through the same real `setLoopPoints()` call instead of bypassing it. Verified live via the debug hook against all four paths (typed Start/End, Suggest, undo, and confirming the pre-existing drag path was already correct): `previewSource.loopStart`/`loopEnd` and the sticky readout now match the boxes immediately after every edit, with no explicit second call needed anywhere. Full 64-test suite (unaffected - this bug has no core/unit-testable surface, it's plugin-internal wiring) and a clean build.
- **BUG FIX, v0.1.214 — saving while on "Saved audio" greyed out the preview toggle (third report of this toggle, a third distinct cause).** The `sound://<id>?variant=clip` handler streams the clip via `fs.createReadStream` + `Readable.toWeb`, which honors backpressure — so for a clip too large for Chromium to buffer whole (reproduced with a 9-minute WAV; a 20s one did *not* reproduce), a loaded `BakedClipPreview` `<audio>` element keeps the file handle open for as long as it's loaded, playing or paused (and `bakedPreview` is preloaded even in Live mode). The re-bake's `renameWithRetry` (`loopClip.js`, ~3s budget) then fails with `EPERM` on Windows → `setLoopClipStale` → `loopClipReady = false` → `updatePreviewModeToggle` forced Live mode and disabled the toggle. Reproduced live via CDP in a throwaway `--user-data-dir` profile: a direct `renderLoopClip` while the preview played returned `EPERM ... rename` after 3.7s. Fixed in `performSave` by disposing `bakedPreview` (releases the handle) *before* `renderLoopClip`, capturing its position/play state first and restoring them on the fresh instance afterward; a `_clipRebaking` flag keeps `updatePreviewModeToggle` from kicking the user out of Saved mode during the window where `bakedPreview` is null. Separately, per the owner's direct requirement ("it has to ALWAYS change between live preview and saved audio preview"), the toggle is never `disabled` any more: `togglePreviewMode()` (now async) switching into Saved with no usable bake runs `save({ forceBake: true })` first (`forceBake` makes `performSave` bake even with no unsaved changes, e.g. after a previous failed render), and only stays on Live with a status message if that render genuinely fails. Verified live: never-saved sound → toggle enabled → click renders and enters Saved; three consecutive saves while playing in Saved mode stayed in Saved, kept playing, `loopClipReady` true; the 9-minute sound saved cleanly in Saved mode (1.7s) where the old path hit `EPERM`. Full 71-test suite and a clean build.
- **Stereo pan (v0.1.216)**, owner inbox: "a draggable bar that makes the sound go either to the left channel or the right channel... avoid breaking export with this and making it slower." Stored as `filters.pan` (-1..1, `0` = center), so it rides every existing filters path for free: overrides, `updateFilters`, the Remix snapshot/dirty check, bake staleness (`loopClipEligible`/`bufferBakeSnapshot` compare whole `filters` JSON), and the Mixer's stream-mode `hasFilters` reconcile. **One pan law** lives in `src/shared/pan.js`'s `stereoPanMatrix` (plugin copy in `plugins/editor/audio/PanStage.js`, keep in sync). It works on a stereo pair (mono is treated as L = R = M at full level): moving toward one side folds the far channel into the near one, power-normalized, and fades the far side on an equal-power curve. Center is exactly the identity. This was chosen over `StereoPannerNode`'s own law, which boosts a hard-panned mono sound by +6 dB; this law tops out at +3 dB (constant power) and keeps wide/uncorrelated stereo steady. **Live**: `PanStage` (`src/renderer/audio/PanStage.js`) is a splitter → 4 gains → merger with its input forced to `channelCount 2`/`explicit`, so mono is up-mixed at unity. It sits after filters/echo/reverb/fluctuation and before the per-sound volume in `LocalFileSoundSource`, in `StreamFilterChain` (scatter/scheduled stream fallbacks), and in Remix's `PreviewSource` (after the envelope). Buffer sources and `BakedClipPreview` need nothing, since pan is baked into the clip. **Bake**: `loopClip.js`'s `renderCrossfadedLoop` takes `pan` and appends `panFilter.js`'s `buildPanFilter` as its very last stage: in the plain `-af` path, or as `[out]pan…[panned]` in the rotated `filter_complex`. On `doRender`'s pre-pass path that puts it after reverb/envelope, matching live order; pan is memoryless, so running it after the crossfade/rotation is equivalent. It needs the input's channel count (`probeChannelCount`, an ffmpeg stderr parse via `parseChannelCount`, only run when pan ≠ 0) because ffmpeg's own mono→stereo conversion is -3 dB, so mono gets explicit summed-row coefficients rather than `aformat`. Export and composites both go through `renderClipToPath` → `doRender`, so they get pan with no extra pass; a centered sound's render is byte-identical to before. Effect presets don't define `pan` and `setFilterSliders` skips an undefined one, so a preset click keeps the pan; `NEUTRAL_FILTERS` (Reset filters / fresh load) re-centers it. Sound mode only (no group/whole-mix pan). Verified against real ffmpeg (mono and stereo channel RMS at 9 pan values within 0.05 dB of the matrix) and live via CDP in a throwaway profile: the Remix preview's L/R levels matched the matrix (0.1246/0.0521 vs 0.1243/0.0520); the saved clip matched within 0.001 dB (-18.112/-25.689 dB); reverb + pan baked through the pre-pass path; a real >10-min stream-mode Mixer source measured a 0.8617 level ratio against 0.8614 expected, and re-centering reconciled live. Presets keep pan, and double-click re-centers it. New `test/panFilter.test.js`; 84-test suite and a clean build.
- **Pan drift (v0.1.217)**, owner request right after v0.1.216: "This should be able to fluctuate the same way volume and pitch do. It would override the sound-set ones if the sound is under a group and the group has it enabled." A third Fluctuation axis, `fluctuation.pan` (-1..1, neutral 0, same axis shape and `normalizeFluctuationAxis` as volume/pitch; default spread -0.5..0.5, bias 0), plus a pan axis on a Sound Group's `filters.fluctuation`. `presets.js`'s `normalizeMixFluctuation(input, { allowPan })` now normalizes each axis via `normalizeMixAxis`; only `normalizeGroupFilters` passes `allowPan`, so a whole-mix pan axis is dropped (no UI for it). **Semantics.** A sound's drift is a *second* `PanStage` (`driftPanStage`) right after its static-pan stage. Reason: a baked clip already carries the static pan in its samples, so buffer mode can only add drift after it, and stream mode, the Remix preview and the export all do the same so the modes can't disagree. Tradeoff: stacking two stages of the pan law isn't the same as adding the pan values (e.g. a hard-left sound drifting further left gets 3 dB quieter). It only matters when both are far off-center. A group's drift is a `PanStage` + `Modulator` in `SoundGroupChain` between `fluctGain` and the limiter, so the whole group moves together. **Override.** While a group's pan drift is on, its members' own pan drift is disabled; their static Pan stays. Live: `tabs/mixer/index.js`'s `effectiveFluctuation(entry)` feeds every source construction and `reconcileSource`. It reads the group's live Remix preview filters (`state.groupFilterPreviews`, set by the `sound-group-preview` listener, which reconciles all sources only when the group's pan switch flips) before its saved filters. The previews are cleared on preset load and on `sound-groups-changed`, which now also reconciles. `loadPreset` reconciles once more after installing groups, since its sounds start before the groups exist. Export: `exportMix.js`'s `memberFluctuation(sound, groups)` does the same when building loop shots and in the progress budget. Remix Sound mode shows a note (`panDriftOverridingGroup()`) on a sound whose group does this; its own preview still drifts, since the preview never routes through groups. **Live.** `SoundFluctuation` (both `Modulator.js` copies) gains `panMod` + an `onPan` callback, and `fluctuationKey` includes `pan`. `SoundSource`, `BufferSoundSource` and Remix's `PreviewSource` each add a `driftPanStage`. **Export.** `panDrift.js` (Electron-free, `test/panDrift.test.js`) samples the same walk at 1000 Hz (`samplePanWalk`, another deliberate copy of the Modulator walk, like gain/pitch envelope). `panMatrixFrames` turns each value into the 4 pan-matrix coefficients (all 0..1) and `encodePanWav` writes them as a 4-channel 16-bit control WAV (`panEnvelope.js` puts it in export-tmp). `panDriftGraph` applies it with channelsplit → 4× `amultiply` → 2× `amix normalize=0` → `join`. That was verified against the real bundled ffmpeg before building on it: hard-left and identity segments measured exactly, and 10 minutes of pink noise cost +1.6s. `driftOutputArgs(preChain, envPath, panPath)` is the one shared builder for "input 0 → volume envelope → pan drift" and is used by all three paths of `tileAndDriftLoopShot` (varispeed stdin, fused rubberband fallback, and a new pan-only tile). `applyEnvelopeToTrack` became `applyBusDriftToTrack`, which does a group's volume + pan drift in one pass (`hasBusFluctuation`). Budgeting counts pan as one more fused axis (`replacedPasses`, `loopAxisPasses`), and a group needs one bus-drift pass if either axis is on. `ENVELOPE_INPUT_CHAIN` was folded into `driftOutputArgs`. **Verified.** 96-test suite (new `panDrift` + group/whole-mix pan normalization tests) and a clean build. The real `exportMix()` ran under plain Node with Electron stubbed (the v0.1.213 harness): no drift → balance exactly 0 dB; pan-only, pitch+pan (varispeed) and volume+pan → balance swinging roughly -25..+30 dB; fixed hard-left drift → right channel silent; group override (member hard-left, group +0.5) → -5.9 dB, exactly the matrix prediction for the group alone; group random pan+volume drift swings; progress reached 100%; no temp files left. Live via CDP in a throwaway profile, with temporary `window.__mixerDebug`/`__remixDebug` hooks removed before commit: the Remix preview's drift stage moves (spread 0.6), the edit auto-saves `fluctuation.pan`, and a buffer-mode and a >10-min stream-mode Mixer source both drift. A live group preview with pan drift stops the member's pan modulator (its drift stage at 0), the group's stage moves (spread 1.4), and turning it off restarts the member; the same override holds for a saved group. The Remix group checkbox drives all of this, the bar's values reach the Mixer preview, a pan-only group edit lights Save, and the save persists. Also fixed the Fluctuation hint text, which still claimed drift isn't baked into exports (true only before v0.1.171).
- **Group "each sound on its own" drift, per-play pan, and Bias toggles (v0.1.218)**, the owner's follow-up to pan drift: a toggle on every group drift bar that hands the group's settings to each member "so that they all behave differently"; Random Interval/Scheduled members use those min/max/bias values as their per-play random range; per-play randomization for every drift axis, including pan; per-play controls redrawn as the same 3-circle bar; and bias as a toggle everywhere it appears. **Rules live in one module, `src/shared/groupDrift.js`,** used by the Mixer, `SoundGroupChain` and `exportMix.js` (`plugins/export/simulate.js` keeps a copy of `applyGroupShotOverride`; `test/groupDrift.test.js` asserts the two copies agree):
  - A group axis with `perSound: true` is dropped from the bus (`busFluctuation`).
  - It replaces each looping member's own axis (`effectiveMemberFluctuation`) and each Random Interval/Scheduled member's per-play fields for that axis (`applyGroupShotOverride`, via `SHOT_AXIS_FIELDS`). Fully random maps to the drift range as explicit bounds, because per-play pitch's own fully-random is ±12 st and drift's is ±6.
  - A group `pitch` axis always counts as per-sound (a bus can't detune), and `presets.js` forces `perSound: true` on it.
  - A shared group pan still disables members' own pan drift (v0.1.217).
  - `normalizeMixFluctuation(…, { groupAxes })` keeps pitch/pan/`perSound` for groups only. Every axis now carries `biasEnabled`, which the whole mix keeps too.

  **Mixer.** `effectiveScatter`/`effectiveSchedule` join `effectiveFluctuation` at every source construction, `reconcileSource` and the test-fire path. The `sound-group-preview` listener reconciles all sources only when `groupDriftMemberKey` (what members receive) changes.

  **Bias off** (`biasEnabled: false`, default true for drift) picks targets uniformly and starts the walk mid-range, in `Modulator` (both copies) and all three export walks (gain/pitch/pan).

  **Per-play pan.**
  - New scatter/schedule fields: `minPan`/`maxPan`/`panBias`/`panBiasEnabled`/`panFullyRandom`, plus `volumeFullyRandom`/`volumeBiasEnabled`/`volumeBias`. All default to "no change", so existing sounds behave the same.
  - `randomPanPosition` and the updated `randomVolumeScale` live in `ScatterSoundSource.js` (Scheduled imports them) and are duplicated in `simulate.js`. Each simulated event now carries `panPosition`.
  - Live: each Buffer/Stream Scatter/Scheduled source has one `PanStage` after its shot gain, re-aimed at each shot. Only one shot plays at a time per source. Like drift, it's a second stage after the static pan.
  - Export: `renderSlicedBatch` gets the shot's channel count (`probeChannelCount`, now exported from `loopClip.js`, probed once per panned sound). Any panned event makes every branch in that batch stereo with `eventPanFilter`. For a mono shot, rows are summed and scaled by −3 dB, so a centered branch matches the batch's normal convert-after-amix level exactly (measured −33.60 vs −33.60 dB). A batch with no pan keeps the v0.1.211 mono passthrough untouched.

  **Remix UI.**
  - `driftAxisTogglesMarkup` adds Bias (and on group bars "Each sound on its own"; locked on for the new group Pitch drift bar) to every drift axis.
  - `shotAxisMarkup` plus `SHOT_AXES` and `readShotAxes`/`writeShotAxes`/`updateShotAxesUI` replace the per-play pitch/volume range sliders with 3-circle bars for Pitch (±12), Volume (0..1) and Pan (±1), each with Fully random + Bias. Old saved per-play pitch/volume values load as-is.
  - `FluctuationBar` gains `setBiasEnabled`, and a `'split'` grab: collapsed min/max are separated by the first drag direction. Without it, a "no variation" default could never be widened to the right.
  - `updateGroupOverrideNotes` explains group overrides in both the Fluctuation and per-play sections. It replaces v0.1.217's pan-only note.

  **Verified.**
  - 116-test suite and a clean build.
  - Real `exportMix()` harness (Electron stubbed):
    - Alternating hard-left/right per-play pan, mono and stereo: every window exact, the opposite side silent.
    - Centered-in-pan-batch level identical to a plain batch.
    - Group per-sound pan reaches a scatter member through the plugin's `simulateEvents` + `applyGroupShotOverride` path, and a loop member (bus unpanned).
    - The v0.1.217 scenarios still pass; no temp files left.
  - Live via CDP (throwaway profile, temporary `__mixerDebug`/`__remixDebug` hooks removed before commit):
    - Remix: Bias toggles default on, and a bias-off volume drift persists. Legacy per-play pitch 3..5 with bias 4 loads into the bar. Real pointer drags split a collapsed pan bar both ways and the result saves. Fully random disables the bias toggle.
    - Mixer: a buffer-mode scatter source's pan changes between shots.
    - Group preview with per-sound pan/volume/pitch: the loop member's own modulators take the group ranges, the bus pan/volume modulators stop, and the scatter member's per-play ranges take the group values (its next shots land inside them). Shared pan restores the bus and disables member pan/pitch; no group drift restores the member's own.
    - Group UI: pitch per-sound is locked on; unchecking pan per-sound restarts the bus, enables Save, and saving persists; both override notes render.
- **Self-contained by design, not by accident**: a plugin can't import files outside its own directory (enforced by the `plugin://` path-traversal guard), so `plugins/editor/` has its own copies of `AudioEngine.js`, the waveform-drawing code, `LoopEditor.js` (the drag/scrub/snap canvas controller), and `util/time.js` — duplicated from core rather than shared. This matches Obsidian's "bundle your own dependencies" plugin model; keep it in mind if core's copies of these change and the plugin's need the same fix.
- `MAX_BUFFER_CLIP_SECONDS` (600s) is duplicated as a local constant in `plugins/editor/index.js` for the same cross-directory-import reason — keep it in sync with `src/shared/constants.js` if that value ever changes.
- **Parametric EQ**: a fixed 7-band peaking-style EQ, folded into the *existing* `filters` object as `filters.eq: [{freqHz, gainDb, q, type}, ...]` (`library.js`'s `defaultEqBands()`) rather than a new top-level field — reuses every bit of existing filters plumbing (staleness comparison, `updateFilters`, live-update paths) with zero new schema/IPC code. `plugins/editor/ui/EqEditor.js` is the draggable-node canvas (X = log-frequency, Y = gain, mouse wheel over a node = Q); `index.js` also exposes precision numeric fields (Freq/Gain/Q) for the selected band. Presets (Muffled/Phone/etc.) never touch `.eq` — only "Reset filters" or a per-sound "Reset EQ" button do. Live preview inserts a chain of `BiquadFilterNode`s between the lowpass and filter-gain nodes in both `PreviewSource.js` and core's `LocalFileSoundSource` (`src/renderer/audio/SoundSource.js`), same position ffmpeg's bake applies them (`equalizer=f=…:width_type=q:w=…:g=…` in `loopClip.js`'s `buildFilterChain`) so preview and the real Mixer agree.
- **Sticky playback bar**: Play/Pause, a loop toggle, a progress bar/time readout, and a preview-volume slider live in `mountSticky()` (see the Plugins section above), not the scrollable panel body — always visible under the tab bar regardless of scroll position. Progress is relative to the *trimmed loop region* (`loopStart`..`loopEnd`), not the whole original file. `mount()` and `mountSticky()` populate the same shared `this.els` object; ordering matters (`mount` then `mountSticky`, both synchronous, see `TabHost.js`) since `wireControls()` (called from `mount()`) doesn't wire the Play/Pause or loop-toggle listeners itself — those are wired in `mountSticky()`.
- **EQ graph backdrop**: translucent bars behind the EQ curve showing roughly how much energy the sound actually has around the EQ's reference frequencies, independent of wherever the user later drags a node — a scoped-down alternative to a literal live spectrum analyzer or waveform overlay (a time-domain plot doesn't belong on this graph's *frequency*-domain X-axis). `src/main/ffmpeg/bandEnergy.js`'s `computeBandEnergy` runs one `bandpass`-filtered ffmpeg pass per frequency (in parallel — wall-clock cost is roughly the slowest single pass, not the sum), computing RMS in JS from the piped PCM. Static, load-time-only analysis (`plugins/editor/index.js`'s `loadSound()`, fire-and-forget) — not tied to playback, not recomputed as the user drags EQ nodes. Two things worth knowing if you touch this: the output PCM sample rate needs to be well above twice the highest reference frequency (a prior version used 11025Hz, which silently discarded real signal at higher bands via ffmpeg's own anti-aliasing filter during resample — now 32000Hz), and a bandpass filter centered at or above the *source* file's own Nyquist frequency is a degenerate configuration that produces meaningless output — probe the source's real sample rate first and skip any band at or past that file's Nyquist entirely.

### The Remix tab: Sound / Preset / Group modes (`plugins/editor/`, tab titled "Remix")

The Remix plugin also covers preset- and group-level editing, alongside per-sound editing. One tab, a three-pill mode switch at the top:

- **Sound mode** — the full per-sound editor: waveform + trim, Doppler, Speed/Pitch/Reverse, scatter/schedule randomization, Fluctuation (slow random volume+pitch drift), Volume envelope (drag control points on the waveform to shape volume over time), Noise gate, Noise reduction, the parametric EQ, HP/LP/Gain/Echo/Reverb filters, effect presets, undo/redo, A/B compare, autosave, and a Live-edit / Saved-audio preview toggle. None of this applies to more than one sound, so none of it shows outside Sound mode.
- **Preset mode / Group mode** — only the filter + EQ modules: HP/LP/Gain/Echo/Reverb, volume Fluctuation, the *same* parametric EQ module Sound mode uses (`EqEditor.js`'s `createEqEditorController` — one shared implementation across all three modes), effect presets, Reset filters — plus the minimal picker/membership chrome each needs (a preset `<select>`; Group mode adds a group `<select>`, "+ New group", a Members checklist, a name field, Delete, and a "Copy to preset…" picker for duplicating a group's filters/EQ/membership into another preset).

**Bus processing** lives in `src/renderer/audio/WholeMixChain.js` (a preset's whole-mix) and `SoundGroupChain.js` (one Sound Group) — each a `highpass → lowpass → EQ → gain → [parallel echo tap] → [parallel reverb tap] → fluctuation gain → brick-wall limiter` chain, installed by `AudioEngine` when a preset loads or groups change. A **Sound Group** is a named subset of one preset's sounds (`presets.js` `groups: [{id, name, soundIds, filters}]`, one group per sound max) routed through that shared chain instead of straight to `masterGain`. The Mixer's right-click menu toggles group membership without leaving the Mixer; both entry points write via `presets:updateGroups` and stay in sync through the `noctivago:sound-groups-changed` CustomEvent. A group's `filters` also supports an `occlusion` (0..1) field — `applyOcclusionToFilters` (`src/shared/constants.js`) log-interpolates the lowpass cutoff down and pushes reverb wet/size up together as occlusion increases (muffled *and* echoey reads as "behind a wall"; muffled alone just reads as quieter), combined with a manual Lowpass by taking whichever is more occluded. **BUG FIX (v0.1.215)**: reverb used to combine the same way (`max(manual, occlusion)`), which silently deadened the manual Mix/Size sliders anywhere below occlusion's own amount (at occlusion 1: Mix < 0.4, Size < 900ms) in both the live `SoundGroupChain` and the export (both call the one shared function). Reported as "reverb controls ain't working properly... something is impeding new changes from applying"; the owner's real active preset ("Rain Inside") had group "Thunda" at occlusion 1 with Mix 0.25, below the 0.4 floor, so every Mix edit from 0 to 0.4 was inaudible. The Remix-to-Mixer event bridge (`noctivago:sound-group-preview`) was checked and was fine. Reverb now uses `pushToward(manual, sliderMax, amount)` (`manual + (max - manual) * amount`, where amount is `0.4 * occlusion` for Mix and `900/4000 * occlusion` for Size; `REVERB_SIZE_MAX_MS = 4000` mirrors the Remix slider's max). That gives the same result as before when manual is 0, and output rises strictly with the manual value at any occlusion. The tradeoff: existing groups with both occlusion and manual reverb sound wetter than before. Lowpass kept `min()` (not reported; at occlusion 1 any manual cutoff above 700Hz is still dead - a known remaining dead zone). Unit-tested, including a regression test; 73-test suite and a clean build.

**Live spectrogram / status in Preset/Group mode**: a plugin has no reference to the Mixer's real `AudioEngine`, so the plugin dispatches a *synchronous* `noctivago:request-analyser` (or `request-mixer-status`) CustomEvent with a mutable `detail` object and reads it back the instant `dispatchEvent()` returns — the Mixer's listener sets `detail.node = engine.wholeMix.analyser` / `engine.groupChains.get(id)?.analyser` before returning. Safe because plugin and core renderer code share one JS realm (only the main-process boundary is actually sandboxed here).

**Export baking mirrors all of this**: `exportMix.js`'s `buildWholeMixPostChain` / `buildGroupFilterChain` / `renderGroupBuses` / `finalizeWholeMix` apply the same HP/LP/EQ/Gain/Echo/Reverb/volume-Fluctuation/occlusion a group or whole-mix carries. Reverb (`afir` convolution) and volume-Fluctuation (a baked `amultiply` gain envelope) each need their own separate ffmpeg pass; the rest is one linear `-af` chain. A per-sound loop sound's own volume **and** pitch Fluctuation bake too (`pitchEnvelope.js` → an `asendcmd` file stepping `rubberband`'s `pitch`, capped at a maximum command-file line count to stay well clear of a real ffmpeg bug in its own `asendcmd` parser on very large command files — see the ffmpeg version pinned in `package.json` before assuming this is fixed upstream), applied to its tiled full-duration track alongside the volume-Fluctuation `amultiply`; pitch-Fluctuation is per-sound only (a bus has nothing single to detune). A sound's Volume envelope bakes via the same WAV+`amultiply` machinery as volume-Fluctuation (`src/main/ffmpeg/volumeEnvelope.js`, `gainEnvelope.js`'s shared `writeWav`/`GEN_RATE`) rather than `asendcmd`, since it needs no continuous re-sampling loop — it rides along for free with every export since it bakes directly into the same short loop-clip WAV every other bake-only filter already uses.

**Export progress reporting must be wired into every real ffmpeg pass, not just the "big" ones** — `exportMix.js`'s `makeReporter` weights the Export tab's progress bar by audio-seconds of work (`step()`/`tick()`/`completePhase()`), and any pass long enough to matter that skips this looks indistinguishable from a hang: this exact bug has been hit and fixed three separate times for three different passes (v0.1.150 for the video re-encode and for Sound Group bus rendering, and again 2026-09-14 for `combineTracks`/`reduceTrackCount`'s "Merging N track groups…" pre-combine step, reported directly as a "fail log" after an export sat frozen on it for 37 real minutes). When adding a new ffmpeg-backed export phase, budget its expected seconds into `totalWorkSeconds` up front and wire its `onProgress` into `reporter.tick`/`completePhase` from the start rather than assuming a short pass — `combineTracks` had gone unwired since the pre-combine rounds were first added because each individual chunk combine had seemed cheap, until a dense enough export (243 tracks in one Sound Group) made it the single longest phase.

**BUG FIX (v0.1.210) — the "Merging N track groups…" pre-combine step wasn't just slow to *report* (v0.1.150/2026-09-14's fixes above), it was doing genuinely far more ffmpeg work than it needed to.** Root-caused from a direct owner report ("there's no way an export to audio should take this long"), not a live debugging session — the installed app wasn't running at the time, but the per-step `export-log.txt` this pipeline already writes (v0.1.154) turned out to be exactly the evidence needed: a real recent export spent 33m35s of its ~49-minute total on a single "Merging 245 track groups…" line, dwarfing every other phase combined. `reduceTrackCount` chunked `combineTracks`' input tracks by raw array position, with zero regard for each track's `.offset` (its absolute position in the export timeline) — and that array has no time locality at all, since Phase 2's `batchJobs` pool runs across every event sound's batches at once to keep concurrency saturated, so array order is really *completion* order. A ~120-track chunk drawn that way, when the export has several Random Interval/Scheduled sounds with batches spread across the whole timeline, ends up containing tracks whose offsets span nearly the *entire* export — so `combineTracks`' own `amix=duration=longest` had to render an output nearly as long as the whole export just to hold a scattering of a few seconds of real audio each, on every single combine pass. That's the exact "N × full duration" cost the v0.1.99 pipeline rewrite was built to eliminate in the first place, silently reintroduced one level up by this later pre-combine step. Made worse by `combineTracks` always hardcoding its result's `.offset` back to `0` regardless of where the chunk's content actually started, baking in leading silence from t=0 even when every track in the chunk started, say, 40 minutes in. Fixed two ways together: `combineTracks` now delays each track relative to the chunk's own *minimum* offset and returns that minimum as the result's real offset (rather than always `0`) — every consumer of `.offset` (the final mixdown, a later merge round, `renderGroupBuses`) already `adelay`s by whatever `.offset` says, so a track that's honestly positioned was already handled correctly, it just never used to *be* honest. `reduceTrackCount` now sorts a copy of its input by `.offset` before chunking, and chunks into groups of a new, much smaller `MERGE_CHUNK_SIZE` (32) rather than reusing `MAX_INPUTS_PER_PASS` (128, which stays exactly as-is for its own real job — capping the *final* pass's input count under the Windows command-line length limit, unrelated to this). Smaller, time-sorted chunks mean each combine pass only has to span its own narrow slice of the timeline instead of nearly the whole thing — more, far cheaper passes instead of few, enormous ones — and this benefit is unconditional (helps even with "Faster export" off, since it cuts total CPU work, not just wall time; concurrency on top of that is pure upside). Verified against the real bundled ffmpeg binary via two Node harnesses (no Electron needed — `resolveFfmpegPath()` just returns the raw `ffmpeg-static` path in dev): (1) a correctness pass — 48 short synthetic tracks scattered across a synthetic 900s timeline, reduced to one final track through multiple real merge rounds under the new logic, then `volumedetect`-probed at every original track's true offset (real signal, confirming the offset math threads correctly across rounds) and at known gaps between tracks (near-silence) — passed cleanly; (2) a scale pass matching the real bug's shape — one 128-track combine spanning a full synthetic hour (matching `MAX_INPUTS_PER_PASS` exactly, mirroring the real 245-track case's own first chunk) took 25.6s real ffmpeg time, versus the same 128 tracks split into 4 time-sorted 32-track chunks (`MERGE_CHUNK_SIZE`) taking 5.9s total *sequentially* (a 4.3x cut in real CPU work) — and since the real pipeline runs those 4 chunks concurrently under "Faster export" (up to `FASTER_EXPORT_CONCURRENCY_CAP`), real wall-clock for that round would drop to roughly the slowest single chunk (~1.7s), a ~15x wall-clock win on this synthetic case alone. Full 65-test suite and a clean build; not verified against a real multi-hour export end-to-end (would take hours to confirm 1:1) — the owner's next real slow export producing a new `export-log.txt` is the natural confirmation this actually lands as expected in practice.

**Export pipeline optimization pass (v0.1.211)** — owner asked for the export to be "the absolute fastest it can possibly be"; everything below was **benchmarked against the real bundled ffmpeg over repeated runs before changing anything**, and two hypotheses were measured and *rejected* rather than shipped on intuition. Rejected: (a) the FLAC `INTERMEDIATE_CODEC` is already right — `-compression_level 0` made no difference at all, and raw `pcm_s16le` is a wash overall (faster encode, but ~3x the bytes makes it *slower* to read back, and a merge pass reads 32+ inputs), so FLAC's smaller files genuinely win; (b) splitting the final mixdown into parallel time segments + concat measured only 1.73x even at 8 segments (I/O-bound, serial concat), which doesn't justify the seam/phase-alignment risk on stateful filters — the existing doc comment's skepticism about time-chunking holds up. What actually shipped, in order of measured impact: **(1) The concurrency cap.** `FASTER_EXPORT_CONCURRENCY_CAP = 4` was a fixed number justified by "a single 20-event rubberband batch was measured at ~500 MB" — a figure predating the v0.1.99 rewrite that made every batch graph `apad` only to its own span (the `EVENT_BATCH_SIZE` comment already conceded peak memory was now "meaningfully lower than even that measurement", but the cap was never revisited). Measured on the current shape: 12 concurrent 20-event rubberband batches peaked at **~1.6 GB of ffmpeg RSS in total** (~130 MB each, not 500), while a fixed 12-batch workload went from 15675ms at concurrency 4 to 7966ms at concurrency 12 — **1.97x** being left on the floor on any machine with more than 4 cores. Replaced with `resolveConcurrency()`: bounded by cores-1, by `MEMORY_BUDGET_FRACTION` (0.5) of total RAM at `PER_PASS_MEMORY_BUDGET_MB` (512) each, and by a `CONCURRENCY_HARD_CEILING` of 12 — yields 11 on a 12-core/16 GB box, and stays at 1-2 on a small machine, i.e. at or below the old behavior there. The v0.1.85 "took all my RAM" failure mode is still real, so this is still bounded, just by what the machine can take rather than a fixed guess. **(2) BUG: `pitchq=speed` was a ~38% slowdown, not a speedup.** Applied only under "Faster export" and documented as "rubberband's fast (slightly lower quality) mode", it measured *consistently slower* on this ffmpeg build across every repetition and both graph shapes this pipeline uses (20-event batch: 3250ms plain vs 4485ms with it; whole-clip speedPitch: 4900ms vs 5167ms). So the toggle meant to make exports faster was paying a real time penalty *and* requesting lower pitch quality — no tradeoff to weigh, removing it is strictly better on both axes. Removed from `exportMix.js`'s `eventBranchChain` and from `pitchEnvelope.js`'s `pitchCommandFilter` (that one runs over the whole export duration, so it's not a small saving); the now-unused `speedMode` parameter was deleted rather than left dangling. **(3) `channels=together` added to every rubberband call site** (`exportMix.js`'s new shared `RUBBERBAND_OPTS`, `loopClip.js`'s speedPitch bake, `pitchEnvelope.js`) — ~1.12x faster, and it fixes the exact defect already fixed on the live-playback side in **v0.1.112**: shifting L and R independently decorrelates them into stereo smear / comb filtering when summed to mono (`pitchStretch.js`'s own fix note describes the identical mechanism). The ffmpeg path never got that fix; this is it, and it makes export match live. Net on the hot per-event batch path vs what "Faster export" used to run: **~1.55x, at strictly better quality**. **(4) Mono sources stop being upconverted before the expensive stage.** `renderSlicedBatch`'s graph did `[0:a]aformat=channel_layouts=stereo,asplit=…`, so a mono source file — very common for one-shot SFX, and shot clips preserve source channel count since `renderClipToPath` passes no `-ac` — ran every branch's rubberband/volume/fade/adelay at 2x the samples for nothing. Moving the conversion to after the `amix` (safe: every amix input comes from that one `asplit`, so they always share a layout) measured **~1.5x** on a 20-event mono batch (2991ms → 2000ms), and is an exact no-op for an already-stereo source. Combined, the batch-placing phase — the dominant remaining cost after v0.1.210 fixed the merge phase — should land around **3x faster**, more for mono-sourced libraries. Verified against real ffmpeg: `asendcmd` + `rubberband:channels=together` still genuinely steps pitch over time (measured 440Hz early → 329Hz late on a real render, so the command file is still being honored), and the mono-passthrough batch graph produces correct stereo output at the right length with real audio at each event position and silence in the gaps. Full 65-test suite and a clean build. Not verified end-to-end on a real multi-hour export — the next real export's own `export-log.txt` is the natural confirmation.

**Fluctuation-baking optimization (v0.1.212)** — the direct follow-up to v0.1.211, driven by a real before/after `export-log.txt` pair the owner produced. v0.1.211 landed as predicted (batch-placing phase **1m48s → 34s, 3.2x**; whole export 14m01s → 8m54s, 1.57x; the log's own `up to 11 passes at once` header confirming `resolveConcurrency()`), but the same logs showed the remaining cost had moved somewhere v0.1.210/211 barely touched: **"Baking drift into a loop sound" was 57% of the export** (two sounds, 4m17s + 3m46s) with Sound Group processing another 28% — together 85%, while the newly-optimized batch phase was down to 13%. Two independent problems, both fixed here. **(1) Three full-duration passes per drifting loop sound, fused into one.** `tileLoopShotFull` wrote a full-duration tiled track, `applyPitchEnvelopeToTrack` read it back and wrote another, `applyEnvelopeToTrack` read *that* back and wrote a third - three encode/decode round-trips of a multi-GB intermediate for what is one linear chain over one stream. New `tileAndDriftLoopShot` does the whole thing in a single invocation: `-stream_loop` feeding `volume → [asendcmd+rubberband pitch drift] → [amultiply volume envelope]`, preserving the live chain's pitch-then-volume order. Verified against the real bundled ffmpeg as output-identical to the three-pass version (same length, same measured level at the same moment, pitch genuinely stepping 440→547Hz via the command file, volume genuinely drifting 9dB peak-to-trough) at 1.12x - deliberately noted as *modest*, because rubberband dominates the cost rather than the I/O, but it also stops writing two full-duration intermediates per drifting sound, real temp-disk and write-bandwidth savings on a multi-hour export. `applyLoopFluctuation`/`applyPitchEnvelopeToTrack` are deleted as dead rather than left dangling; `applyEnvelopeToTrack` stays live since a Sound Group's *bus* drift genuinely is a separate pass over an already-combined submix. **(2) The bigger win: both drift loops were sequential `for` loops.** Ungrouped drifting loop sounds baked strictly one after another (hence 4m17s *then* 3m46s for two independent renders), and inside `renderGroupBuses` each group's member loop sounds tiled one after another too - the only phases in this pipeline that never went through `mapPool`. Both now do, results written *by index* rather than pushed so mix/submix input order stays deterministic regardless of completion order. The nested case needed care: `renderGroupBuses` already runs groups concurrently, so naively giving the inner loop the full concurrency would allow concurrency² processes (121 on an 11-wide machine) - exactly the unbounded fan-out the old hard cap existed to prevent - so `perGroupConcurrency` divides the budget by group count (verified across a table of group/concurrency combinations that worst-case total stays ≈ concurrency, never squared). Progress accounting was kept honest through the fusion: the reporter budgets one tile plus one pass per active axis for a drifting sound (`fluctuationWorkSeconds`, plus the tile via `groupedLoopSoundCount` for a grouped one), so the fused pass banks `durationSeconds * (1 + activeAxes)` in one `completePhase` rather than leaving the bar permanently short of 100%. Also in this release, owner-requested after comparing two logs by hand: **`export-log.txt` now opens with a `Noctívago vX.Y.Z — export log — <ISO date>` header**, stamped in `ipc.js`'s `export:writeLogFile` from `app.getVersion()` so it reflects the real running build and can't drift from it. Full 65-test suite and a clean build; the next real export's log is the confirmation.

**Tape-style pitch-drift baking + a Sound Group timing regression fix (v0.1.213)** — driven by the first version-stamped log (`v0.1.212`, 1-hour "Rain Inside" export, 7m28s total: Sound Group processing 3m26s = 46%, "Baking drift into 2 loop sounds" 3m22s = 45%). The owner picked both follow-ups offered: (A) Sound Group processing, no sound change, and (B) switch export pitch drift to tape-style. **(B) Varispeed pitch drift.** The live Mixer applies a buffer-mode loop sound's pitch Fluctuation through `AudioBufferSourceNode.detune`, which the Web Audio spec folds into the node's computed playback rate (the codebase already says so in `pitchStretch.js`'s header) — so live drift is tape-style (pitch and speed together), while exports baked it with `asendcmd`+`rubberband` (pitch only). That mismatch was also ~all of the drift phase's cost. New `src/main/ffmpeg/varispeedCore.js` (Electron-free so `test/varispeedCore.test.js` — 6 tests — can run it: rate-1 reproduces the tiled clip sample-for-sample, rate-2 doubles the heard frequency, the rate follows the curve over time, the loop seam wraps without a jump, gain + cross-block state are exact, bad input throws) reads the short decoded loop clip at a time-varying, linearly-interpolated rate (matching Chromium's own `AudioBufferSourceNode` interpolation), fed by `pitchEnvelope.js`'s new `samplePitchRatios` (same walk as before, expressed as rate multipliers). `exportMix.js`'s `tileAndDriftLoopShot` decodes the clip once (`decodeClipToFloat32`, streaming, abandoned early past `MAX_BUFFER_CLIP_SECONDS`), then `renderVarispeedDrift` pipes one-second Float32 blocks into ffmpeg via the new `runFfmpegWithStdin` (in `runFfmpeg.js`; honors stdin backpressure, rejects writes once ffmpeg has exited, kills ffmpeg if generation throws), yielding to the event loop between blocks so the main process stays responsive; ffmpeg only applies the volume envelope (if any) and encodes. This replaces **both** the full-duration tile pass and the rubberband pass. Measured on a real 1-hour bake: **180.1s → 8.2s (21.9x)**, identical length. A clip longer than `MAX_BUFFER_CLIP_SECONDS` (which live can't buffer either) falls back to the previous fused rubberband pass, so `renderPitchCommandFile`/`pitchCommandFilter` stay live as that fallback. Known and deliberately left alone: **stream-mode** live pitch drift (`SoundSource.js`, loop regions over 600s) rides `playbackRate` with the browser's default `preservesPitch=true`, so it's actually a tempo wobble with no pitch change — a pre-existing live bug, logged to the Board rather than fixed here. **(A) Sound Group processing — and a real regression from v0.1.210.** Reading the owner's actual preset (read-only, `%APPDATA%\noctivago\presets.json`/`library.json`) showed the 3m26s wasn't the bus passes at all: group "outside" contains "acampamento sons", a 75s loop with pitch drift, baked *inside* the group step with rubberband (~3 min) — now covered by (B). The bus passes themselves are cheap: a synthetic 1-hour bus with 24 members + filters + a 1.2s reverb + volume drift took 24.3s sequential, and fusing all four passes into one graph measured only **1.34x** — not worth duplicating `applyReverbPass`'s graph inline, so that fusion was deliberately *not* done. While tracing that path, found that **v0.1.210's `combineTracks` change broke Sound Groups**: v0.1.210's entry above claims every `.offset` consumer already honored it, but `renderGroupBuses` passed the combined track to `applyGroupFilters`, which read it as starting at 0 and returned offset 0 — so a group made only of Random Interval/Scheduled sounds was pulled earlier by its first event's time (any loop-sound member, being offset 0, masked it, which is why v0.1.210's harness and v0.1.212's grouped-drift check never caught it). Reproduced first against the real `exportMix()` (shots scheduled at 30s/60s came out at 0s/30s, with and without group reverb and group volume drift), then fixed: `applyGroupFilters` folds `track.offset` in as a leading `adelay` inside the pass it already runs at full length (no extra ffmpeg work), and `renderGroupBuses` now skips the combine for any lone member rather than only an offset-0 one. **Verification harness worth reusing:** the real `exportMix()` runs under plain Node with Electron stubbed via a module resolve hook (`node --import ./register.mjs`, where the hook maps `'electron'` to a stub exporting `app` with `getPath`/`isPackaged`/`getVersion` — every module in the export's dependency tree imports only `app`). Passed: ungrouped drift (varispeed), grouped drift (varispeed via the group bus), a 610s clip (rubberband fallback) — each exactly 120s long, progress reaching 100%, pitch measurably moving, no temp files left behind — plus the group-timing regression in all three group configurations. Full 71-test suite and a clean build. Expected on the owner's preset: the two ~3.5-minute phases drop to seconds each.

**Export destination filename** (v0.1.192): `ipc.js`'s `export:pickDestination` still auto-creates a per-preset subfolder named after the preset alone (so re-exporting the same preset lands in the same place), but the *file* inside it now carries more than just the preset's name — the Export plugin appends the video kind (when exporting video at all) and a compact export length, e.g. `Rainy Night - Black screen - 9h.wav` (inbox request). `fileName` is a new, separate param from `defaultName` (still the bare preset name, used only for the folder) on that same IPC call; the video's own filename automatically follows since `exportMix.js` derives it by swapping the audio path's extension for `.mkv`.

**`plugins/editor/` is self-contained** by the `plugin://` path-traversal guard — it carries its own bundled copies of `AudioEngine.js`, the waveform-drawing code, `LoopEditor.js`, `Modulator.js`, `NoiseGate.js`, `util/time.js`, and `MAX_BUFFER_CLIP_SECONDS` — duplicated from core, not shared. Keep the copies in sync if core's change.

### Presets as primary context: per-preset sound overrides

A preset's `sounds[]` items are `{soundId, volume, overrides}`, where `overrides` is `null` (pure inheritance from the sound's shared baseline) or a bag of the overridable keys named in `src/shared/constants.js`'s `OVERRIDABLE_SOUND_KEYS` (trim, filters/EQ, crossfade, Speed/Pitch, play mode, scatter/schedule config). Editing a shared sound while one preset is loaded only changes that preset's own copy — switching to a different preset shows its own separately-saved version, or plain baseline if that preset never touched it. The merge (`applySoundOverride(entry, override)` in `src/shared/constants.js`) is the single source of truth for this, shared by `library.js` (bake-eligibility) and `tabs/mixer/index.js` (the live resolution layer); `plugins/editor/index.js` and `plugins/export/index.js` each keep their own duplicated copy per the plugin sandbox's can't-import-outside-its-directory rule. `Fluctuation` is deliberately excluded from the override system — it's architecturally separate (own debounce, excluded from Remix's undo/snapshot system) and isn't a Save-button-driven field.

The app always has at least one preset loaded — `presets.js`'s `ensureDefaultPreset` creates a "Default" preset (seeded from whatever's currently included, if migrating from an older version with no presets at all) if none exists, called from `src/main/index.js`'s startup bootstrap. Deleting the last remaining preset auto-creates a replacement seeded from the deleted preset's own sounds/overrides, so a preset is always active.

## Plugin ecosystem note

`plugins/export/` (mix export/rendering to a file) and other first-party plugins follow the same self-contained, `plugin://`-loaded pattern described above. See each plugin's own `manifest.json` and `index.js` for specifics — the patterns described for the Remix plugin (self-contained copies of shared logic, live-preview-vs-bake split, staleness checks reusing the `filters` object) generalize to any plugin doing similar work.

## Freesound import

Search/preview/import sounds from [Freesound.org](https://freesound.org) directly into the library. The search/browse UI itself lives in the **Browse Sounds plugin** (`plugins/browse-sounds/`, see below) as of v0.1.204 — everything in this section below the UI layer (auth, the main-process search/download/proxy code, the actual import) is core and unchanged by that move.

**Auth**: a single app-wide API token (`FREESOUND_API_KEY`, requested once at freesound.org/apiv2/apply), baked in at build time (`electron.vite.config.js`'s `__FREESOUND_API_KEY__`, main process only). This is Freesound's simplest auth shape and covers everything this feature needs — text search and downloading a sound's **preview** (compressed mp3, not the original file). OAuth2 (a per-user login flow) is only required for write actions (upload/rate) or for downloading a sound's full original-quality file, neither of which this feature does — importing a preview is plenty for a looping ambient sound and keeps the whole feature to one token instead of a login flow. It's baked into the one build `.github/workflows/release.yml` produces, which goes to both the GitHub release and itch.io unmodified — it's a free, re-issuable, read-only key rather than a real credential, so the owner accepted it being extractable from a public binary.

**Main process** (`src/main/freesound/`): `client.js` does the search call and builds an authorized (token-appended) preview URL; `download.js` reuses the bundled ffmpeg (`runFfmpegToFile`, same shape as `library.js`'s own direct-link download) to pull a preview down to a scratch wav; `protocol.js` registers a `freesound-preview://` privileged scheme (alongside `sound://`/`plugin://` in `src/main/index.js`) that proxies live preview *auditioning* through an authorized fetch — the renderer only ever sees the plain, unauthenticated preview URL Freesound's search response returned, and the API token never crosses into the sandboxed renderer. `authorizedPreviewUrl`'s hostname check doubles as an SSRF guard against a URL that isn't actually pointing at Freesound.

**Import** (`library.js`'s `addSoundFromFreesound`) downloads the preview and funnels it through the exact same `addSound(keepCopy: true)` path every other import already uses, plus one new field: `source: { type: 'freesound', freesoundId, username, license, pageUrl, importedAt }`, carrying the attribution a CC-BY (or similar) sound legally needs — surfaced as a small "Freesound" badge in the Mixer (`SoundRow.js`, reusing the Composite badge's styling) whose tooltip shows the author/license.

### The Browse Sounds plugin (`plugins/browse-sounds/`, tab titled "Browse Sounds")

A dedicated tab for internet-sourced sound discovery, split out of the old "Search Freesound…" add-sound-menu dialog in v0.1.204 per the owner's own direction: browsing/discovering sounds you don't yet know needs fundamentally more screen space and richer UI than importing a file you already have and know (which is exactly why the old dialog felt "underbaked" and "not a pleasant experience for browsing"). Local file/folder/watch-folder/record imports explicitly stayed in core's existing "+" menu, unchanged — only internet-sourced search moved here, and the old dialog (`src/renderer/core/FreesoundDialog.js`, its `#freesound-dialog` markup, its `.freesound-*` CSS, its "Search Freesound…" menu item) was deleted outright rather than kept as a second entry point.

- **Nothing new on the backend** — `window.noctivago.freesound`/`window.noctivago.library` (the same `search`/`isAvailable`/`addSoundFromFreesound` the old dialog called) is already the full API surface a plugin needs, handed through wholesale per the plugin architecture's own `app.noctivago = window.noctivago` rule. The only main-process change was adding `description` to `client.js`'s `SEARCH_FIELDS`/mapped result shape, since the richer cards needed it and the old dialog never had.
- **Card grid instead of a list** (`grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))`) so the extra room actually gets used, and every field the search response already returns now shows: tags (capped at 6 + a "+N" overflow pill), a 2-line-clamped description, and a rating (`★4.3 (12)`) the old dialog silently discarded despite the API already sending it.
- **Infinite scroll**: an `IntersectionObserver` on a sentinel `<div>` after the results grid triggers `loadMore()` (guarded by `loading`/`hasMore`/an active query) instead of the old dialog's manual "Load more" button — replaces `runSearch({append: true})`'s trigger, not its logic, which is otherwise identical to before.
- **A sort picker** (best match / highest rated / most downloaded / longest / shortest / newest) the old dialog never exposed even though `searchSounds`'s own `sort` param already supported all of these — a real, if small, missed feature the redesign picked up along the way.
- **State persists across tab switches for free** — a tab's `mount()` only runs once (`TabHost.js`'s lazy-mount-once-then-show/hide lifecycle), so the current query/results/scroll position survive navigating away and back, unlike the old modal which reset every time it opened.
- **A source badge on every card** (`.browse-card-badge`, reusing the exact amber `.sound-row-composite-badge` color the Mixer's own Composite/Freesound badges already use) — built before a second source existed so the layout wouldn't need to change when one landed; it landed a day later (see below).
- Verified live via CDP against a real dev build: the tab registers and mounts correctly, the old menu item and dialog are confirmed gone, and — since no `FREESOUND_API_KEY` was available in this local dev environment to exercise a real search — `renderCard()` was verified directly against synthetic result data (long-name ellipsis, 8-tag truncation to 6+"+2", a populated vs. a zero-rating result, an empty description omitting the `<p>` entirely) plus the preview button's play/pause toggle. Full 64-test suite (unaffected - `client.js`'s existing search tests don't assert the full mapped shape) and a clean build (bundle size dropped, confirming the old dialog's code was actually removed, not just unreferenced).

**YouTube search added (v0.1.206)**, after a 2026-09-15 research pass (owner interview + Board) found the official YouTube Data API is a dead end for this app's one-app-wide-key model (the Freesound pattern): `search.list` costs 100 of a project's 10,000 daily quota units, so a single key would cap *every user of the app combined* at ~100 searches/day, and a quota increase needs a Google compliance audit (weeks to months, not guaranteed). Instead, `src/main/ytdlp/search.js` reuses the yt-dlp binary already bundled for "Add from link"'s video-site fallback (since v0.1.132) — it searches with no API key and no quota at all, the same way the site itself works rather than calling Google's API. `--flat-playlist --dump-json` against `ytsearch{page*pageSize}:<query> --playlist-start <n> --playlist-end <m>` returns one NDJSON line per result with everything a card needs (title, duration, channel, thumbnail, description, watch URL) already in one call — verified against the real bundled binary before writing this (2.2s for 20 results); no second per-video yt-dlp call needed. `--playlist-start`/`--playlist-end` against a larger `ytsearchN:` count works as a genuine page offset (also verified directly) — YouTube search has no cursor/token the way Freesound's API does, so each "page" re-runs the search up through its own end index rather than continuing a saved cursor, which is fine at the scale an interactive search realistically reaches. Reuses the same `ytDlpCookiesBrowser` setting "Add from link" already has for YouTube's bot-detection, rather than a second parallel cookie setting.

The owner's own direction building this: "do it in a way that won't break the sound browser... contained inside the plugin but if it doesn't work or breaks it doesn't stop the other half (freesound)." Freesound and YouTube are two fully independent sources in the plugin's own state (`this.sources.freesound`/`.youtube`, each its own `available`/`page`/`hasMore`/`loading`/`error`) - every YouTube-specific call (`isSearchAvailable`, `searchYouTube`, a card's own render) is wrapped in its own try/catch so a yt-dlp failure never blocks, clears, or otherwise touches Freesound's results, and vice versa; verified live by confirming a search with no `FREESOUND_API_KEY` locally still returned a full YouTube grid with the correct "Freesound isn't available... YouTube still works" banner and zero console errors. Results from both sources combine into one grid (owner's own settled answer from the day before: "search both sources at once, combined, tagged by source," not a *source-picker* toggle that only ever shows one source's results at a time — v0.1.207 below adds an *include/exclude* toggle per source, a different axis, see that entry) - `renderCard(sound, source)` branches only on rendering shape (Freesound's username/license/rating vs. YouTube's channel/thumbnail), everything else (grid, sentinel, infinite scroll) is shared. YouTube results get a thumbnail and a "Watch ↗" link (opens externally via the app's existing `setWindowOpenHandler` → `shell.openExternal`) instead of Freesound's in-app preview button, since there's no cheap streaming preview URL the way Freesound's `previews` field provides - importing is the only way to actually hear one. Import reuses `library:addSoundFromUrl` unchanged (the exact same yt-dlp-download pipeline `AddLinkDialog.js` already drives), capped at the same 10-minute default; since its progress channel is a single global stream with no per-request id, only one YouTube import can run at a time (every other YouTube card's Import button disables while one is in flight - the same constraint `AddLinkDialog`'s own single-download dialog already lives with), while Freesound imports are unaffected since they use a separate channel. A `seenYoutubeIds` Set filters cross-page duplicates - found live while testing infinite scroll (two consecutive `ytsearch` calls at different end-indices can rank a video differently between calls, producing an occasional repeat at a page boundary; Freesound's API has a stable cursor and doesn't have this issue). Verified live via CDP against a real dev build with real network access (no Freesound key needed for this half): a real search returned 12 YouTube cards with correct thumbnail/channel/duration/watch-link; scrolling to the sentinel loaded 12 more with the dedup fix confirmed (24 cards, 24 unique URLs); a real import of an actual search result through the unmodified `addSoundFromUrl` pipeline produced a real library entry (cleaned up after). Full 65-test suite and a clean build.

**Per-source enable/exclude toggles (v0.1.207)**, owner inbox follow-up the day after v0.1.206 shipped: "Youtube search as well as freesound search should be toggles." A checkbox per source (`#browse-source-freesound`/`#browse-source-youtube`, both checked by default) next to the search bar - unrelated to the combined-grid decision above (that's still in effect: both sources still render into one shared grid when both are on). Each source gained an `enabled` field alongside its existing `available`/`page`/`hasMore`/`loading`/`error` state; `search()`/`loadMore()` now gate on `available && enabled`. Unchecking a source removes its cards from the grid immediately (`toggleSource()` queries `[data-source="key"]` and removes them, same selector `fetchSource`'s own non-append path already uses) and marks it `hasMore = false`; re-checking it re-fetches page 1 fresh into the existing grid via the same `fetchSource(key, {append:false})` path `search()` uses, rather than requiring a whole new search. `checkAvailability()` also disables and force-unchecks a toggle for a source that isn't available at all, so you can't "enable" a source that can't work. **Bug caught live while verifying this**: re-enabling YouTube initially returned only 1 of 12 expected results - `toggleSource`'s re-enable path called `fetchSource` without clearing `seenYoutubeIds` (the cross-page dedup set added in v0.1.206), so every video from the *previous* search was still marked "already seen" and filtered out as a false duplicate. Fixed by clearing `seenYoutubeIds` on every toggle of the `youtube` key (both directions - off needs it cleared too, since its cards are gone from the grid either way). Verified live via CDP against a real dev build: toggling YouTube off correctly cleared the grid to 0 cards with a "turn on at least one source" status; toggling it back on correctly re-populated with no duplicates (11/11 unique URLs, confirmed after the dedup fix). Full 65-test suite and a clean build.

**Freesound name-as-link, and a sticky preview playback bar (v0.1.208)**, owner inbox 2026-09-16, three asks in one note: (1) a Freesound card's name should link out to the sound's real Freesound page, styled as a link with a small link icon; (2) a draggable progress bar so a preview can be scrubbed forward/back instead of only playing start-to-end; (3) "a topbar similar to the one on the remix tab but without the audio editing modules, just the ones that control playback and volume." All three are Freesound-specific (YouTube results already open externally via their own "Watch ↗" link and have no in-app preview to control). The card's name is now an `<a href={pageUrl} target="_blank">` wrapping a truncatable `.browse-card-name-text` span plus a small Feather-style external-link SVG icon, `target="_blank"` routed externally the same way as the YouTube "Watch ↗" link already is. `mountSticky()` (newly registered for this plugin, alongside the existing `mount()`) renders a stripped-down echo of the Remix plugin's own sticky bar — Play/Pause, a draggable progress-bar-with-thumb, a time readout, and a volume slider, all `browse-sticky-*`-prefixed duplicates of the same CSS shapes `plugins/editor/styles.css` already has (per this plugin's own self-contained-by-directory rule) — with none of Remix's editing modules. `togglePreview()` (the existing per-card ▶/⏸ button handler) now also updates the sticky bar's sound name and enables its Play button; a new `applyPreviewPlayState(playing)` keeps the card's own button and the sticky Play button in sync from every call site (card click, sticky Play click, `stopPreview()`); `wireStickyProgressDrag()` is a simplified copy of Remix's own mousedown/mousemove/mouseup seek handler (no loop-region math needed here, just plain 0..`audio.duration`); `updateStickyProgress()` reads `previewAudio.currentTime`/`.duration` on `timeupdate`/`loadedmetadata` events. Verified live via CDP against a real dev build (a temporary `window.__browseDebug = this` hook, removed before commit): a synthetic Freesound card's name rendered as a real `<a>` with the correct `href`/`target`/`rel` and the icon present; starting a preview correctly populated the sticky name and flipped both the card and sticky Play icons; a real `mousedown` dispatched at 75% along the progress bar's own bounding rect set `audio.currentTime` to within rounding of 75% of a (stubbed) duration, confirming the drag-to-seek wiring itself fires correctly, not just the underlying method; stopping reset every sticky element back to its idle state. Full 65-test suite and a clean build.

**BUG FIX (v0.1.209) — self-review pass over v0.1.204-208 (8 parallel finder agents, backlog was dry of anything owner-actionable) caught a real toggle-off race in the per-source enable/exclude toggles (v0.1.207).** `toggleSource(key, false)` clears a source's cards and sets `hasMore = false`, but a `fetchSource()` call already in flight for that key when the toggle fires has no way to know it's now stale — its only staleness guard was `requestId`, which only changes on a fresh `search()`, not a toggle. Three independent review agents converged on the same repro: search with both sources on, disable the slower one (YouTube, mid-`yt-dlp` search) before its response lands, and the late response would silently re-append its cards and flip `hasMore` back to `true` — undoing the toggle-off the user had just performed, with no error and no visible cause. A related, subtler case (also flagged independently): a rapid off-then-on toggle before the pre-toggle fetch resolves could start a second, concurrent `fetchSource()` for the same key, racing the first and risking duplicate/stale cards depending on resolution order. Fixed with a per-source `generation` counter (mirroring the existing global `requestId` pattern this file already uses for cross-search staleness) — `toggleSource(key, false)` bumps `sources[key].generation`, and `fetchSource()` captures it before its awaits and bails if it's changed by the time the response lands, regardless of the source's `enabled` state when it resolves (a fresh fetch from a subsequent re-enable already owns the current generation, so the stale one is correctly discarded even if the user turned the source back on in between). Also extracted two small pieces of logic that were independently duplicated in both `toggleSource()` and `fetchSource()` (the sentinel-visibility check, the per-source card-clear query) into `updateSentinelVisibility()`/`clearSourceCards()`, added a module-level `SOURCE_KEYS` constant replacing 5 repeated `Object.keys(this.sources)` calls, and dropped a dead `s.hasMore = true` write in the toggle re-enable path (unconditionally overwritten by the very next `fetchSource()` call before ever being read). Not live-verified via CDP — no existing Electron/CDP test harness in this repo, and building one from scratch was judged disproportionate to a self-review cleanup pass (the same call made in prior self-review passes, e.g. v0.1.205); instead verified directly against the real, unmodified `BrowseSoundsPlugin` class via a hand-rolled minimal-DOM Node harness with controllable delayed search promises — confirmed the harness genuinely reproduces the bug against the pre-fix code (stale card reappears, `hasMore` flips back on, sentinel un-hides) before confirming the fix resolves it, plus a second scenario for the rapid off→on double-fetch case (exactly one card survives, from the fresher call, no stuck `loading` state). A handful of lower-severity/theoretical findings from the same review pass (YouTube's `hasMore` computed after filtering out live-stream results, which could rarely end infinite scroll one page early; a confusing retry message if "Add folder" partially fails after files are already imported; minor cross-file pagination-clamp duplication between `client.js`/`search.js`) were not chased this pass — none were confirmed-reproducible regressions, matching the usual bar for what gets fixed immediately versus logged as a follow-up. Full 65-test suite and a clean build.

## Community presets (v0.1.219)

Owner request: users upload presets from the app, and other users browse and import them. The owner made three decisions:
- **Audio:** "mix of both". Freesound sounds are re-downloaded by id, and everything else is uploaded.
- **Moderation:** a report button, with no approval queue.
- **Backend:** left to Claude.

**Backend: Cloudflare Worker + D1 + R2**, in `community/`. This is its own npm package with its own `wrangler`. Nothing in it is packaged into the app, because electron-builder only ships `out/**`.
- **Why Cloudflare:** R2 has 10 GB free and charges no egress. Supabase's free tier has 1 GB of storage and 5 GB of egress, and it pauses a project after a week of inactivity. Uploading audio made egress cost the deciding factor.
- **Identity:** no accounts. Each install generates a random 32-byte author key (`src/main/community/client.js`, stored in its own `community` electron-store) and sends it as `X-Author-Key`. The server stores only its SHA-256, and only that key can update or delete its own uploads.
- **Abuse limits:** 10 uploads and 30 reports per day, counted per salted IP hash. After 3 distinct reporters, a preset is hidden automatically. `/v1/admin/*` routes (Bearer `ADMIN_TOKEN`) list, hide, unhide, or delete presets.
- **Upload caps:** 50 MB per preset (a free-plan Worker can't receive a body over 100 MB anyway). Uploads are refused once stored bundles total 8 GB (`maxTotalBytes`, checked before any R2 write). R2 is the only part that can bill and has no spending cap; the owner explicitly worried about surprise charges, so this keeps it inside the 10 GB free tier. Workers and D1 are on the free plan, which just stops at its limits.
- **Deploy:** the owner does it once; steps are in `community/README.md`. Until then, `COMMUNITY_API_URL` (a repo *variable*, read at build time into `__COMMUNITY_API_URL__`) is empty, and the Community plugin doesn't register its tab.
- **Local server for unpackaged builds:** set `NOCTIVAGO_COMMUNITY_URL`.

**The transfer format is the existing `.ncvpreset`, with additive fields.** Old app versions ignore the new fields, and old files still import.
- **What was missing before:** `presetPortable.js` never exported Sound Groups or per-preset overrides, so any exported preset lost them. This was a pre-existing gap and is fixed for local exports too.
- **New fields:** `sounds[].overrides`, `sounds[].source` (the Freesound attribution), and `groups[]` with members stored as indexes into `sounds[]`. `presetPortableCore.js` (Electron-free, unit-tested) has `remapGroups`, which assigns fresh group ids and enforces one group per sound. `finalizeImport` is now async; it saves overrides and groups and returns `failedCount`.
- **Freesound sounds:** `buildPortableBundle(id, { skipFreesoundAudio })` omits their audio. On import, such a sound resolves in one of two ways:
  - It matches a library entry with the same `source.freesoundId`. The sender's overridable settings then become that preset's overrides, so the recipient's baseline is untouched.
  - Otherwise it's `fromFreesound`: the import downloads it with `freesound/client.js`'s new `getPreviewUrl(id)` plus the existing `addSoundFromFreesound`.
- **Size:** `community/bundle.js` re-encodes every uploaded sound to Opus 96k. Loop points, envelopes and schedules are in seconds, so they stay valid. A 6-second WAV went from 1.0 MB to about 80 KB.
- **Shared import path:** `prepareImport(buffer)` does read → analyze → stash for both a local pick and a community download.

**App side.**
- The main process handles every server call (`src/main/community/client.js`, IPC `community:*`, preload `window.noctivago.community`). The renderer never talks to the server directly.
- The `plugins/community/` tab has three views: Browse (search, sort by newest or most downloaded, infinite scroll, Import, Report), My uploads (Update… re-publishes from any local preset, Delete), and Share (pick a preset, name, author, description, tags, and a required rights checkbox).
- Import reuses the Mixer's own dialog. The plugin dispatches a synchronous `noctivago:open-preset-import` event with the analysis. The Mixer sets `detail.handled` and later dispatches `noctivago:preset-import-finished`.
- The dialog shows a "Downloads from Freesound" status for those sounds.
- All server-provided text is rendered with `textContent`.

**Verified.**
- 134-test suite (new `test/presetPortableCore.test.js` and `community/test/validate.test.js`) and a clean build.
- A scripted API walkthrough against local `wrangler dev`:
  - Rejections: no author key, no rights confirmation, a non-zip file, and another author's update or delete.
  - Search and bundle: literal `%` search, tag search, no secret columns in responses, and a byte-exact bundle round-trip.
  - Counters and moderation: download counting, 3 distinct reporters hide a preset (a repeat reporter isn't double-counted), wrong admin token rejected, unhide, delete.
- The real built app in a throwaway `--user-data-dir` profile, driven via CDP (no playback). The Share UI refused to publish without the rights box, then showed each progress step and "1 sound uploaded, 1 linked from Freesound". The card rendered `<b>` in a tag as text. Import through the card opened the Mixer dialog ("Included in this file" / "In your library") and created a preset with its group, gain, volumes, overrides, and a `.opus` library file. Report, My uploads, Update (renamed on the server) and Delete all worked.

**Server went live (v0.1.220).** The owner deployed on 2026-09-17 with Claude driving wrangler: D1 `noctivago-community` (its id is in `wrangler.toml`), R2 bucket `noctivago-community`, and Worker URL `https://noctivago-community.noctivago-community.workers.dev`. `ADMIN_TOKEN` and `IP_SALT` are set as Worker secrets; the owner keeps the admin token. The `COMMUNITY_API_URL` repo variable is set, so v0.1.220 is the first build that shows the Community tab.

Deploying `community/` changes: run `npx wrangler deploy` from `community/` (already logged in on the owner's machine). Schema changes go through `npm run db:remote`.

Testing gotcha: Cloudflare rejects any request that sets `CF-Connecting-IP` itself (403). A multi-reporter hide can only be tested against local `wrangler dev`, not production.

The production API walkthrough passed (a single reporter is counted once and doesn't hide), and test data was cleaned up afterwards.

## Watch folders

`src/main/watchFolders.js` auto-imports any audio file dropped into a folder the user has registered (`library.js`'s `addWatchedFolder`/`listWatchedFolders`), via `fs.watch` while running and a rescan (`library.js`'s `scanWatchedFolder`) on every startup to catch anything that arrived while the app was closed (`fs.watch` only reports live changes). A newly-seen file waits for its size to stop changing across two spaced checks before importing (`checkStable`), so a large file still mid-copy-in isn't imported truncated.

**Subfolders are watched recursively and interpreted as tags (v0.1.198)**, owner-requested to mirror the existing manual "Add folder as tag" dialog but automatically and per subfolder level rather than one flat tag the user types for a whole flat root: `<watched root>/Rain/Heavy/thunder.wav` gets tagged both `Rain` and `Heavy`; a file dropped directly into the watched root itself gets no tag (nothing to derive, matching the manual dialog's own requirement that the user supply one). `src/main/watchFolderTags.js`'s `tagsForWatchedFile(rootPath, containingDir)` is the one small pure function this rule lives in — split out of `library.js` specifically because `library.js` constructs a real `electron-store` `Store` at module load, which can't be imported by a plain `node:test` run outside Electron; keeping the actual path logic in its own dependency-free module is what makes it unit-testable at all (`test/watchFolderTags.test.js`). The live watcher passes `{ recursive: true }` to `fs.watch` (well-supported on Windows, the only platform this app targets) and derives tags from the relative path Windows hands back in the callback. Only ever tags a file at import time — an existing sound already in the library keeps whatever tags it has and is never retroactively re-tagged if later moved between subfolders, matching how a watched folder never touches a sound it's already imported (`importIfNew`'s own dedup-by-path check). Verified live via CDP against a real dev build with a real nested folder (root file, one level, two levels) — the initial scan tagged all three correctly, and a file dropped into a subfolder while the app was already running was picked up by the live recursive watcher with the correct tag, matching the initial-scan behavior exactly. Full 60-test suite (5 new) and a clean build.

**Hardened initial-scan walk against a folder cycle and against blocking the main process (v0.1.201)**: the initial scan (`scanWatchedFolder`, run both when a folder is first added and, via `startWatching()`, on every app startup) originally walked the whole subtree in one `fs.readdirSync(path, { recursive: true, withFileTypes: true })` call — flagged in a self-review (not a reported bug) as having two latent weaknesses on a large or unusual real folder: (1) `{ recursive: true }` has no cycle protection, so a Windows directory junction or a OneDrive "placeholder" reparse point pointing back at one of its own ancestors would make it walk forever; (2) it's fully synchronous, so a very large or deep folder would block the main process's event loop (and every pending IPC call) for the whole scan. `src/main/folderWalk.js`'s `walkFilesRecursive(rootPath, onFile)` replaces it: an async, `fs.promises`-based depth-first walker that resolves each directory's real (symlink/junction-resolved) path via `fs.realpath` and skips one it's already visited, and yields to the event loop (`await new Promise(setImmediate)`) between directories so a big scan is chunked rather than one long freeze. Its own dependency-free module (same reasoning as `watchFolderTags.js`) so `test/folderWalk.test.js` can unit-test it directly against a real filesystem, including a real Windows junction cycle (`fs.symlinkSync(..., 'junction')`, which doesn't require admin rights) — confirms the walk terminates and doesn't double-count files rather than just trusting the logic by inspection. `scanWatchedFolder` and `addWatchedFolder` are now both `async` as a result.

**Manual "Add folder as tag/preset" import now matches watch-folder import too (v0.1.203)**: `addFolderSounds` (`library.js`) used to be a flat, single-level `readdirSync` with no tag derivation at all - it now reuses the same `walkFilesRecursive` + `tagsForWatchedFile` treatment `scanWatchedFolder` already has, so both the "Add folder as tag" and "Add folder as preset" dialogs (`tabs/mixer/index.js`) pick up nested files and per-subfolder tags automatically, the same way a watched folder does. Resolved the one open design question this left behind: whether the dialogs' own explicit typed tag should apply on top of the new auto-derived subfolder tags or replace them - both call sites already applied their tag *additively* (`updateTags(id, [...entry.tags, tag])`, pre-dating this change), so the answer was already implicit in the existing code rather than a real toss-up: a folder named "Rain" containing a "Heavy" subfolder now ends up tagged "Rain" (typed) *and* "Heavy" (derived), never a forced choice between them. `addFolderSounds` is `async` now for the same reason `scanWatchedFolder` is. Verified live via CDP against a real dev build with a genuine nested test folder (root file, one level, two levels) - `addFolderSounds` alone produced the correct per-file tags, and simulating the dialog's own additive `updateTags` call on top confirmed the explicit tag layers correctly rather than overwriting. Full 64-test suite (unaffected - this path has no existing unit coverage, mirrors `scanWatchedFolder`'s own already-tested logic) and a clean build.

**BUG FIX, v0.1.205 — self-review pass over v0.1.201-204 (8 parallel finder agents) caught a real data-integrity bug in v0.1.203's own `addFolderSounds` change, plus two smaller robustness gaps, none reported.** (1) `addFolderSounds` re-inlined `importIfNew`'s own filter/`addSound`/`updateTags` steps line-for-line instead of calling it, which silently dropped `importIfNew`'s `hasSoundForPath` dedup check that `scanWatchedFolder` already relies on for exactly this reason (a watched-folder rescan needs to be a no-op on files it's already imported). Re-running "Add folder as tag" or "Add folder as preset" on an already-imported folder created a full duplicate set of library entries instead of skipping them. Fixed by having `addFolderSounds` call `importIfNew(filePath, keepCopy, tagsForWatchedFile(...))` directly, the same way `scanWatchedFolder` already does — one line replaces four, and the dedup comes back for free. (2) `folderWalk.js`'s `walkFilesRecursive` called `onFile(dirent, dirPath)` with no try/catch — a single file throwing synchronously inside it (a permission error, a file deleted mid-scan, disk full mid-copy) propagated up through every pending recursive `walk()` frame and aborted the *entire* remaining tree, not just that one file, silently dropping every file after it — for both a manual folder import and a watched-folder background rescan. Wrapped in a quiet catch matching the file's own existing `realpath`/`readdir` failure handling, plus a new regression test (`test/folderWalk.test.js`) proving one throwing file doesn't stop the rest of the walk. (3) The two "Add folder" dialogs (`tabs/mixer/index.js`) had no try/catch around the `addFolderSounds` IPC call — an unexpected rejection left the Confirm button disabled and the status stuck on "Adding sounds…" forever, with no way out but Cancel; now caught, the button re-enables, and a plain error message shows. Also fixed the stale "No audio files found directly inside that folder" copy in both dialogs (accurate for the old flat scan, wrong since v0.1.203 made it recursive). Two smaller bugs in the new Browse Sounds plugin from the same review: a failed search wiped whatever results were already on screen instead of preserving them (a real regression from the old dialog's own behavior, introduced when the clear-results call moved earlier in the new plugin's `search()`); and `loadMore()`'s auto-triggered infinite-scroll request had no staleness guard against a fresh search starting before it resolved, which could append a stale query's cards onto a new query's grid — fixed with a monotonic request-id counter, the same shape a debounce/cancellation guard elsewhere in the app already uses. Also hardened an unguarded `sound.avgRating.toFixed(1)` (only checked `numRatings > 0`, not that `avgRating` itself was non-null) found in the same pass. `addFolderSounds`'s dedup fix relies on `importIfNew`, which is already proven via `scanWatchedFolder`'s real production usage — not independently re-verified live via CDP this pass (would have needed a from-scratch Electron-stub test harness this repo doesn't have yet), verified instead via the full 65-test suite (1 new) and a clean build.

## Releasing and auto-update

`npm run release:win` (`electron-vite build && electron-builder --win --publish always`) builds the Windows installer and publishes it as a GitHub Release, via `electron-builder.yml`'s `publish` config. A GitHub Actions workflow can run this automatically on any pushed tag matching `v*`, using GitHub Actions' own auto-provided `GITHUB_TOKEN` to create the release and upload the installer.

**In-app update checking** (`src/main/autoUpdate.js`, `electron-updater`) checks GitHub Releases on every startup of a *packaged* build (`app.isPackaged` gate — never runs under `npm run dev`), and only ever prompts before acting: a "Download?" dialog on `update-available`, then a separate "Restart now?" dialog on `update-downloaded`. `autoUpdater.autoDownload`/`autoInstallOnAppQuit` are both `false` for exactly this reason — nothing happens silently.

If this repository is public, `electron-updater`'s GitHub provider can read releases with no authentication at all — no token needed. (If forking this project to a private repository, `electron-updater`'s GitHub provider needs a token to read releases; see its own documentation for the `GH_TOKEN` environment variable it reads at runtime — avoid baking a broad-access token into a shipped binary, use a narrowly-scoped, read-only token instead.)

**itch.io** gets the exact same installer the GitHub release just published, pushed via Butler in the same workflow run (`.github/workflows/release.yml`, skips itself gracefully until `ITCH_BUTLER_API_KEY` is set as a repo secret). This used to be a second, separate build made deliberately without a private-repo update token so a working repo-clone credential wouldn't end up on a public download page — now that the repo itself is public and that token is gone entirely (see the note above), there's nothing left to keep the two builds apart, so it's one build, published twice.

## Status

Noctívago implements: a mix/preset model (per-sound "include in mix" toggle, global play/pause, global volume, per-preset sound overrides), the ffmpeg-based buffer-mode/waveform system (with an automatic self-crossfade on every baked loop clip), Opus/mp3/wav/ogg/flac/m4a format support, a plugin architecture (tab shell, `plugin://` loading, fail-soft per-plugin loading), the Remix plugin (trim, filters, parametric EQ, Doppler, Speed/Pitch/Reverse, scatter/scheduled randomization, Fluctuation, Volume envelope, Noise gate/reduction, whole-mix and Sound Group bus processing with occlusion), export/rendering to a file, in-app rename, an app-wide double-click-resets-any-slider convention, a dedicated Browse Sounds tab for Freesound search/preview/import with attribution (more sources planned), and CI-built installers with in-app update checking.

See `CHANGELOG.md` for the detailed, dated release history.
