node_modules/
.wrangler/
out/
dist/
.claude/settings.local.json
# Downloaded by scripts/download-ytdlp.mjs (repo "postinstall") rather than
# checked in - same reasoning ffmpeg-static's own binary isn't checked in
# either, just fetched on install.
resources/bin/

# Local Worker secrets (wrangler dev)
.dev.vars

# Build output of scripts/build-plugins.mjs (plugins written in JSX) -
# regenerated from plugins/<id>/src/ by every dev/build run.
plugins/*/main.js
plugins/*/islands/
